# Research OS vNext Failure-Injection Traceability Matrix

This matrix traces the 20 required failure classes in §26 of
`RESEARCH_OS_VNEXT_META_SPEC.md` to implementation and direct negative tests.
The original table below is a retained static source-review mapping, not a
current execution report. Its old line numbers are historical navigation hints;
use the named owners/tests and current evidence reconciliation below.

## September 23 V9 execution status — incomplete

September 23, 2026 checkpoint, incorporating the Python 3.14 terminal observation at 16:19:20 UTC: V9 is installed and frozen with 300 functional inputs.
The same 125 selected IDs pass on Python 3.14.6 and 3.11.15 at enclosing scope;
a disjoint 3,294-ID selection passes on Python 3.14.6 only. This is 3,419 distinct
selected current IDs on Python 3.14, with only 125 currently paired; remaining192
on Python 3.11 is NOT_STARTED. Nested/helper behavior remains NOT_ASSESSED by
the enclosing closures. These are not whole-suite, security, scientific or
release acceptance.

Exact source identities and both enclosing closure digests are in
[final verification](VNEXT_FINAL_VERIFICATION.md). The original20-row table
remains a retained static source/test mapping, not20 current runtime passes.
Separate wrappers, component tests and direct integrations have different
coverage; enclosing success does not establish unassessed nested execution.

The original `tests/test_scientific_core.py` methods named in row16 remain
outside the current final selection because that whole module is excluded:
`test_omitted_evidence_use_cannot_grant_scientific_writer_eligibility`,
`test_default_writer_view_excludes_eligible_nonscientific_claims`, and
`test_serialized_claim_class_flip_cannot_reuse_support_receipts`.
The original `tests/test_gates_paper.py` methods in rows5,9,16 and the
paper/Soundness cross-cutting rows are also outside the ordinary selection;
gates67 retains paired/diagnostic7,200-second timeouts and UNKNOWN cause.
These are different exclusion reasons, not a claim that every gate is stopped.
Existing safe wrapper/companion modules are their own enclosing scopes, never
whole-module substitutes or extra original IDs. No current result here closes
the excluded originals or authorizes wrapper expansion/retesting.

The Standard source scan is sealed PARTIAL, with 43 review products and three
LOW findings. Accepted corrections are installed and have scoped test evidence;
this is not full security PASS or clearance of omitted/stopped scopes.

The exact safeguard-stopped assess-patch-risk and substitutes, prompt-injection
criterion, scholarly event-key/capture-alias, direct-coordinator legacy-selector/
native-J retained session/thread/fork/FD lifetime, execution/read-restriction
repair, trusted-collector and actual OS-crash collection scopes remain stopped,
not PASS. No alternate reviewer, route, runtime or diagnosis clears them.
Protected data, confidentiality boundaries, custodian requirements, spending
limits, scientific gates and human-only E4 remain mandatory. No credential,
private provider/run payload, protected dataset or unrelated process/descriptor
inspection is authorized here.

## September20 captured-evidence reconciliation — historical

The following paragraph/table preserve the September20 evidence record. Their
“current,” “running,” “pending,” “uninstalled” and runtime statements refer to
that date, not V9. Old counts/digests/outcomes are unchanged and are not transferred
to changed current bytes or added to V9 selections.


The current paired selection contains 1,340 distinct passing IDs on each actual
runtime (Python 3.14.6/3.11.15), reconciled from retained captured reports, not a
single full-suite invocation. Report SHA-256:
`dfc562c922f9596bd3eb9a3497dd7b938d68442323a7aa5f27b7cb1749a8dca4`.
The ten-method snapshot module is excluded for one Python 3.11 AST-dump
portability failure; its reviewed test-only correction remains uninstalled.
Resource4 has two passes/two failures per runtime in installed v3; private v4
passes four/four but is not installed. E2E8 passes Python 3.14.6 and paper-edge3
passes Python 3.11.15; their other-runtime counterparts are running and neither
batch is included in the paired headline. Additional regressions remain in
progress. Historical364, reconstructed146/architecture4, and the two additional
recovered-baseline checkpoint controls remain separate evidence populations.

| Required rows | Current executed evidence and scope |
| --- | --- |
| 1–3 citation/claim/metadata | Selected `test_literature`19 passes both runtimes, including the three exact negative tests named below. Offline structural evidence only, no real scholarly source or semantic reviewer authority. |
| 5–7,10,12–15,17,19–20 | Selected `test_scientific_design`51 passes both runtimes: frozen direction/units, fabricated or missing ablation, baseline omission, evaluator exploitation, unfair budgets, seed omission, weak superiority, novelty collision, post-hoc timing and proxy-scope refusals. These are synthetic component controls, not genuine protected confirmation. |
| 11 leakage | Selected `test_domains`40 passes both runtimes, including generic split/preprocessing, medical patient/session/site and recommender interaction/session negatives; no real protected dataset is used. |
| 8 missing/stale/modified artifact | Existing selected missing-source replay refusal (`test_real_missing_stored_source_private_and_public_refusals_are_identical`), corrected-manuscript refusal (`test_unrelated_append_is_accepted_but_correction_is_rejected`) and tampered-content/source-map refusal (`test_tampered_content_or_source_map_is_rejected`) pass. These cover paper-source/revision controls, not every experiment-output owner. |
| 18 failed reproduction | Existing selected `test_mechanical_comparison_pass_fail_and_outside_tolerance_are_non_evidentiary` passes. It directly checks an exact mismatch→`FAIL` and0.8 versus0.7 at frozen tolerance0.01→`OUTSIDE_TOLERANCE`, with scientific evidence false. No real scientific clean rerun is inferred. |
| 4 and integrated7 | Existing altered-score and fabricated-ablation child-program cases pass in the eight-method captured E2E selection on Python 3.14.6 (result `c9816a6594104048ae37eced99848374a85f3e0dc5f3de85a0cf36900370ad56`). Python 3.11.15 remains running. These are synthetic local-child checks, not scientific confirmation or isolation clearance. |
| 9 method/code or table/prose mismatch | The installed three-case paper-semantic selection includes the original metric and method/asset negative cases. All three passed Python 3.11.15 in the captured launcher; Python 3.14.6 is still running. Positive source-map rendering alone is not substitute evidence. |
| 16 qualitative overclaim | Selected composition rejects a `QUALITATIVE`/`SYSTEM_FIXTURE` claim from scientific composition. The explicit registered-evidence strength-overstatement case passed within the same three-case Python 3.11.15 selection; Python 3.14.6 remains pending. Proposal-slot uniqueness is not strength evidence. |

Whole-module native/custody/security tests are not authorized merely by this
mapping. Stopped investigations remain stopped. Other unselected tests are
unassessed, not automatically failed or externally blocked. Exact current
results and unresolved boundaries remain in `VNEXT_FINAL_VERIFICATION.md`.

## Retained static mapping

Status semantics:

- `PASS` means the inspected tree contains both a fail-closed implementation boundary and a direct test that injects the named failure. It does **not** mean this review executed that test.
- `PENDING` means either the implementation boundary or a direct injection test is absent.
- Full captured-suite execution remains unresolved. Historical counts and current bounded enclosing results do not upgrade static labels into runtime or scientific clearance. Excluded/timed-out originals remain outside current evidence; analogous tests and separate wrappers never upgrade an original method or whole module to executed `PASS`.

| # | Exact meta-spec failure class | Implementation boundary | Direct failure-injection test name(s) | Static traceability |
|---:|---|---|---|---|
| 1 | nonexistent citation | `verify_reference()` stops at L0 when no normalized scholarly record resolves (`src/scientist_one/literature.py:2859-2950`). | `test_nonexistent_citation_stays_level_zero` (`tests/test_literature.py:759`) | PASS |
| 2 | real citation supporting the wrong claim | Exact claim, passage, source, and locator hashes must match and the semantic assessment must support that claim (`src/scientist_one/literature.py:2998-3023`). | `test_different_claim_passage_or_negative_judgment_cannot_reach_level_four` (`tests/test_literature.py:861`) | PASS |
| 3 | wrong DOI or metadata | Identifier resolution is unambiguous before title, author, and year matching can reach L2 (`src/scientist_one/literature.py:2954-2984`). | `test_wrong_doi_title_authors_and_year_never_reach_metadata_match` (`tests/test_literature.py:806`) | PASS |
| 4 | altered experiment score | The integrated analyzer independently reconstructs correctness from the frozen dataset/evaluator, recomputes each accuracy, and compares the result to both artifact and manifest metrics (`src/scientist_one/research_os.py:1641-1785`). | **Real subprocess E2E:** `test_hash_consistent_wrong_manifest_metric_fails_semantic_recomputation` (`tests/test_research_os_e2e.py:412`) | PASS |
| 5 | wrong metric direction | `metric_improvement()` uses the frozen metric direction; paper assertions must match the authoritative direction (`src/scientist_one/scientific_design.py:1609-1632`; `src/scientist_one/paper_pipeline.py:399-417`). | `test_metric_direction_is_authoritative_not_writer_selected` (`tests/test_scientific_design.py:670`); `test_wrong_metric_value_unit_and_direction_fail_closed` (`tests/test_gates_paper.py:263`) | PASS |
| 6 | percent/fraction and time-unit mismatches | Metric observations must have exactly the frozen unit; there is no implicit conversion (`src/scientist_one/scientific_design.py:1609-1626`). | `test_percent_fraction_and_seconds_milliseconds_never_implicitly_convert` (`tests/test_scientific_design.py:684`) | PASS |
| 7 | fabricated ablation | Promoted ablation bytes must contain a recomputable binary vector, exact frozen bindings, the declared intervention, and baseline-equivalent result; contract validation also rejects unknown/missing ablations (`src/scientist_one/research_os.py:1888-1934`; `src/scientist_one/scientific_design.py:2720-2749`). | **Real subprocess E2E:** `test_hash_consistent_fabricated_ablation_without_vector_fails_closed` (`tests/test_research_os_e2e.py:456`); component: `test_fabricated_and_missing_ablation_results_fail_closed` (`tests/test_scientific_design.py:1035`) | PASS |
| 8 | missing, stale, or post-evaluation-modified artifact | The local backend confines and hashes every output, binds seed/ablation references, and revalidates the captured set during reconcile (`src/scientist_one/experiments.py:2173-2250`). Canonical state refuses missing or stale parents (`src/scientist_one/research_state.py:1947-1966`). | `test_stale_or_altered_output_is_revoked_after_success` (`tests/test_discovery_experiments.py:675`); `test_materialization_rejects_missing_stale_and_wrong_code_dependencies` (`tests/test_vnext_state.py:953`) | PASS |
| 9 | method/code or table/prose mismatch | Paper verification checks numeric source/value/unit/direction, exact method/code bindings, and authoritative asset parents (`src/scientist_one/paper_pipeline.py:399-425`). | `test_wrong_metric_value_unit_and_direction_fail_closed` (`tests/test_gates_paper.py:263`); `test_method_code_and_asset_parent_mismatch_fail_closed` (`tests/test_gates_paper.py:287`) | PASS |
| 10 | omitted strong baseline | Every `MUST_RUN` baseline needs execution or a strong evidence-bound exclusion; a central superiority claim needs an executed baseline (`src/scientist_one/scientific_design.py:1844-1890`). | `test_omitted_must_run_baseline_blocks_superiority` (`tests/test_scientific_design.py:703`) | PASS |
| 11 | test-set, patient, session, or site leakage | Provider-independent domain adapters check generic split/preprocessing leakage, medical patient/subject/session/scan/site separation, temporal order, and recommender interaction/session/user/item separation (`src/scientist_one/domains.py:610-735`; `src/scientist_one/domains.py:1033-1115`; `src/scientist_one/domains.py:1513-1590`). | `test_split_preprocessing_checkpoint_and_budget_leakage_fail` (`tests/test_domains.py:284`); `test_patient_session_scan_site_leakage_and_slice_pseudoreplication_fail` (`tests/test_domains.py:441`); `test_interaction_session_temporal_leakage_and_evaluator_exploitation_fail` (`tests/test_domains.py:543`) | PASS |
| 12 | evaluator exploitation | Typed blocking exploitation signals make a passing evaluator assessment invalid and independently block promotion (`src/scientist_one/scientific_design.py:2674-2717`); domain adapters add evaluator-specific checks. | `test_evaluator_exploitation_signal_is_independently_blocking` (`tests/test_scientific_design.py:1023`); `test_interaction_session_temporal_leakage_and_evaluator_exploitation_fail` (`tests/test_domains.py:543`); `test_proxy_promotion_unfair_runtime_metric_direction_and_evaluator_gaming_fail` (`tests/test_domains.py:655`) | PASS |
| 13 | unfair compute or tuning budget | Candidate/baseline comparison conditions include tuning trials and compute budget and must be equivalent before superiority (`src/scientist_one/scientific_design.py:1635-1725`; `src/scientist_one/scientific_design.py:1844-1890`). | `test_unfair_compute_or_tuning_budget_blocks_comparison` (`tests/test_scientific_design.py:751`); `test_typed_policy_leakage_unfair_resources_and_contamination_fail` (`tests/test_domains.py:312`) | PASS |
| 14 | selective seed reporting | Every frozen seed must have exactly one terminal record; `ALL_SEEDS` cannot select a representative seed, and the experiment manifest must contain every planned seed (`src/scientist_one/scientific_design.py:2030-2056`; `src/scientist_one/experiments.py:2196-2200`). | `test_all_seed_reporting_rejects_selective_omission` (`tests/test_scientific_design.py:857`); `test_selective_seed_output_fails_closed` (`tests/test_discovery_experiments.py:619`) | PASS |
| 15 | statistically weak result framed as superiority | Sample size, meaningful effect, confidence interval, adjusted alpha, and multiplicity correction are mandatory before superiority (`src/scientist_one/scientific_design.py:2167-2186`; aggregate gate at `src/scientist_one/scientific_design.py:2788-2800`). | `test_statistically_weak_result_cannot_be_framed_as_superiority` (`tests/test_scientific_design.py:941`) | PASS |
| 16 | qualitative overclaim | Canonical claims reject expressed strength above evidence-permitted strength and forbid evidentiary strength on unverified claims (`src/scientist_one/research_state.py:1001-1103`). Independently, omitted claim use defaults `NON_EVIDENTIARY`, serialized use is required, support receipts bind the class, and the default writer boundary is scientific-only (`src/scientist_one/claims.py:70-81`; `src/scientist_one/claims.py:378-485`; `src/scientist_one/claims.py:695-705`; `src/scientist_one/claims.py:1143-1206`). | `test_claim_taxonomy_and_evidence_bounded_strength` (`tests/test_vnext_state.py:337`); `test_omitted_evidence_use_cannot_grant_scientific_writer_eligibility` (`tests/test_scientific_core.py:943`); `test_default_writer_view_excludes_eligible_nonscientific_claims` (`tests/test_scientific_core.py:1002`); `test_serialized_claim_class_flip_cannot_reuse_support_receipts` (`tests/test_scientific_core.py:1035`); paper boundary: `test_zero_scientific_writer_claims_make_fixture_candidate_unsupported` (`tests/test_gates_paper.py:256`) | PASS |
| 17 | novelty collision | Typed collision dimensions prevent a conflicting closest prior work from receiving a clear-novelty status, and uncleared novelty cannot promote (`src/scientist_one/scientific_design.py:1209-1341`). | `test_closest_prior_work_collision_cannot_be_labeled_novel` (`tests/test_scientific_design.py:582`) | PASS |
| 18 | failed clean reproduction | Clean reruns require the same scientific binding, every seed, the frozen tolerance, and eligible evidence before returning `PASS` (`src/scientist_one/experiments.py:2644-2714`). | `test_clean_rerun_outside_frozen_tolerance_is_not_a_pass` (`tests/test_discovery_experiments.py:792`) | PASS |
| 19 | post-hoc hypothesis represented as pre-specified | `formed_after_observation` is equivalent to `POST_HOC`; post-hoc hypotheses cannot enter confirmation and result-seen promotions are rejected (`src/scientist_one/scientific_design.py:1367-1450`; `src/scientist_one/scientific_design.py:1498-1541`). | `test_result_observed_hypothesis_must_be_post_hoc` (`tests/test_scientific_design.py:608`) | PASS |
| 20 | proxy metric represented as end-to-end evidence | Metric scope cannot be promoted beyond its contract without an independently verified proxy-to-end-to-end inference; the systems adapter separately detects proxy promotion (`src/scientist_one/scientific_design.py:2759-2785`; `src/scientist_one/domains.py:2085-2175`). | `test_proxy_metric_cannot_be_promoted_to_end_to_end_without_verified_inference` (`tests/test_scientific_design.py:956`); `test_proxy_promotion_unfair_runtime_metric_direction_and_evaluator_gaming_fail` (`tests/test_domains.py:655`) | PASS |

## Cross-cutting authority injections

This is also a retained static mapping. PASS and scoped-review wording below
are not new V9 runtime/security acceptance. No stopped mechanism is assessed or
authorized by this table; excluded original tests remain excluded.

These controls do not add failure classes to the exact 20-row meta-spec matrix. They test custody and promotion edges that can otherwise launder one of those failures through a hash-valid but semantically false authority.

| Authority edge | Fail-closed behavior | Direct injection or readback evidence | Static traceability |
|---|---|---|---|
| Literature investigation -> checked research/novelty gates | Citation expansion persists bounded page and terminal receipts, immutable occurrence-level graph provenance, target-bound ranking, and complete round history. Research and novelty gates reopen exact registry artifacts and reject missing, unrelated, wrong-type, spliced, cycled, budget-truncated, or changed authorities. Synthetic sources remain non-scientific and real literature remains `BLOCKED_EXTERNAL`. | `test_fixture_executes_and_reloads_authoritative_investigation`, `test_registry_research_gate_rejects_unrelated_scholarly_authorities`, and `test_registry_gate_rejects_missing_or_unrelated_reference_artifact` (`tests/test_research_os_literature_integration.py`); citation/round-state injections in `tests/test_literature_investigation.py` | PASS |
| Checked discovery/superiority/paper promotion | Discovery recomputes registered evidence, persists evaluation and promotion receipts, revalidates promoted branches, and retains positive, negative, null, failed, rejected, invalid, and terminated history. The only superiority-authority path recomputes exact contract, eligibility, baseline, aggregate, evaluator, statistic, metric/unit/scope, and compute inputs into an ordered-parent receipt; the value-only API is explicitly diagnostic. Caller-selected paper flags or a diagnostic cannot promote a claim. | `test_checked_result_recomputes_and_promotes_with_frozen_receipt`, `test_promoted_authority_is_revalidated_before_later_consumption`, and negative/null retention cases (`tests/test_discovery_experiments.py`); `test_checked_path_recomputes_and_re_resolves_authoritative_receipt`, `test_value_only_legacy_path_is_explicitly_diagnostic`, and forgery cases (`tests/test_superiority_authority.py`); paper authority cases in `tests/test_gates_paper.py` | PASS |
| Reviewed-template autonomy -> exact completed verifier | Provider output is limited to the two reviewed template enums and bounded parameters. Admission rejects arbitrary code/control fields, unknown templates, path escapes, model/context/evidence substitution, backend subclasses or method overrides, and nondeterministic rendering. Completed verification independently rebuilds the artifact graph, recomputes output semantics, checks ledger/canonical-state bindings, and proves claim/paper exclusion; hash-valid false semantic or network receipts fail. | Adversarial cases in `tests/test_autonomous_implementation.py`; `test_hash_valid_autonomous_semantic_and_network_forgery_fails_closed` and autonomous fields in `test_complete_fixture_rehydrates_all_authorities_and_count_tampering_fails` (`tests/test_vnext_operations.py`) | PASS |
| Adaptive plan job bytes -> registry/ledger custody | Promotion canonically parses `execution-plan.json`, recomputes its typed SHA, and requires equality with submission and collection. It registers a deduplicable raw `adaptive_execution_plan` and a per-run `adaptive_execution_plan_binding` parented exactly to plan and frozen spec; job/run/backend/spec/plan substitution fails before promotion. Both enter captured hashes and the ledger. | `test_plan_bytes_deduplicate_while_each_run_gets_an_exact_binding` and `test_tampered_or_rebound_plan_fails_before_registry_promotion` (`tests/test_research_os_plan_custody.py`); post-collection/rebinding cases in `tests/test_research_os_e2e.py` | PASS |
| Typed scientific status -> terminal outcome authority | The exact 11-value vocabulary has diagnostic mappings; `PASS`/`SUCCESS` is nonterminal. Only source-bound derivations may materialize a canonical terminal/Decision. The GPU owner replays one exact prospective unperformed CUDA-device obligation and emits only an operational blocker; unknown/renamed work, stale/corrected sources, competing recovery slots and changed snapshots refuse. Prospective policy prevents wall-budget fallback. No scientific or spending authority is granted. | Terminal vocabulary/value-only refusal tests (`tests/test_terminal_outcomes.py`); actual progress/renamed-input laundering controls (`tests/test_gpu_validation_requirement.py`); native codec controls (`tests/test_gpu_compute_terminal.py`); real canonical publication/recovery/drift (`tests/test_gpu_compute_terminal_lifecycle.py`) and both-order/policy/drift routing (`tests/test_gpu_compute_terminal_routing.py`) | PASS — scoped review closed; final frozen verification pending |
| Soundness/Challenger inputs -> promotion | Soundness requires all 15 exact dimension receipts and all 14 exact category reviews, with registry-resolved evidence, roles, execution status, and matching findings. Missing/SHA-shaped/wrong-role receipts, missing categories, spliced findings, unexecuted required attacks, and unresolved blockers cannot yield PASS. | `test_missing_or_sha_shaped_dimension_authority_fails_closed`, `test_wrong_role_dimension_receipt_is_not_authority`, `test_challenger_checklist_must_cover_every_exact_category`, `test_finding_must_be_content_bound_to_matching_category_and_evidence`, and `test_positive_selection_integrity_requires_executed_challenger_reviews` (`tests/test_gates_paper.py`) | PASS |
| Scheduled GPU provider -> local registry | The injected-scheduler adapter requires a planned equivalent local/cloud escalation, idempotent provider identity, monotonic state/attempt, checkpoint-bound requeue, idempotent cancel, and a bounded spec-matching returned bundle. Hostile identities, attempt rollback, checkpoint substitution, unplanned submission, and invalid bundles fail closed. All receipts remain `UNTESTED` and non-scientific because no live GPU boundary was validated. | `test_structured_scheduler_lifecycle_and_registry_return_are_untested`, `test_structured_scheduler_requeue_binds_checkpoint_and_attempt`, `test_structured_scheduler_requeue_requires_next_attempt`, `test_structured_scheduler_rejects_unplanned_and_invalid_identity`, `test_structured_scheduler_cancel_is_idempotent`, and `test_structured_scheduler_invalid_bundle_fails_closed` (`tests/test_experiments.py`) | PASS |
| LOCAL_MAC technical completion -> scientific eligibility | A valid output manifest, semantic recomputation, and clean system rerun still cannot establish scientific evidence without a backend-private OS-isolation attestation. No current path creates one; runtime remains `VALIDATED_LOCAL`/`NON_EVIDENTIARY`, `scientific_evidence=false`, and `UNKNOWN_UNATTESTED`. The derived documentation/readiness classification is `BLOCKED_LOCAL`. | `test_caller_evidence_flag_cannot_forge_os_isolation` (`tests/test_experiments.py`); integrated conservative classification in `test_real_local_fixture_materializes_conservative_authorities` (`tests/test_research_os_e2e.py`) | PASS |

## Integration-depth notes

The following descriptions belong to the retained static review, not newly
measured V9 execution. “Current”/“now” and line references below describe that
historical inspection. Current enclosing scopes do not prove nested integration,
production-launcher coverage, scientific eligibility or whole-module completion.

The two bold tests above are the current failure injections that mutate the copied real child program and then traverse the actual `LocalMacBackend`, output manifest, registry, ledger, deterministic analyzer, and failed operation receipt. Their adversarial outputs remain internally hash-consistent; failure occurs at semantic recomputation rather than at a superficial hash check (`tests/test_research_os_e2e.py:412-513`).

The other 18 rows have direct component-level injections, not merely analogous tests. This satisfies static traceability to the required failure class, but it does not claim the same integration depth as the two subprocess cases.

The real-subprocess semantic E2E tests intentionally call `run_research_os_fixture()` as a direct integration-test API in a temporary root (`tests/test_research_os_e2e.py:22-26`, `tests/test_research_os_e2e.py:145-154`). Production instead enters through the captured launcher, CLI, and `_project_command`-guarded orchestrator method (`scripts/scientist_one_cli.py:950-1068`; `src/scientist_one/cli.py:108-109`; `src/scientist_one/orchestrator.py:2782-2788`). `test_complete_fixture_rehydrates_all_authorities_and_count_tampering_fails` invokes the decorated orchestrator method and verifies completed-operation authority rehydration, but it constructs the orchestrator under mocked captured-root admission (`tests/test_vnext_operations.py:59-67`, `tests/test_vnext_operations.py:145-201`). `test_fixture_dispatch_uses_guarded_orchestrator_and_canonical_json` verifies CLI routing with a mocked orchestrator (`tests/test_vnext_cli.py:60`). No one current test is a captured-production-launcher E2E through final rehydration.

Claim evidence-use is a cross-cutting hardening boundary, not a twenty-first failure class. `ClaimEvidenceUse.{SCIENTIFIC,SYSTEM_FIXTURE,NON_EVIDENTIARY}` is serialized in `MaterialClaim`; omission defaults fail-closed to `NON_EVIDENTIARY`; support-receipt schema 1.1 binds that class; and `writer_view()` excludes non-scientific classes unless a caller explicitly requests the diagnostic view (`src/scientist_one/claims.py:70-81`, `src/scientist_one/claims.py:378-485`, `src/scientist_one/claims.py:1143-1206`). Both the vNext fixture and the legacy synthetic claim are explicitly `SYSTEM_FIXTURE` (`src/scientist_one/research_os.py:2714-2723`; `src/scientist_one/orchestrator.py:3573-3579`). The direct vNext E2E readback rehydrates the graph, verifies it, observes an empty default writer view, and sees the claim only in the typed diagnostic view (`tests/test_research_os_e2e.py:274-301`).

Operation receipts are also cross-cutting, not a twenty-first failure class. Atomic reservation and immutable `IN_PROGRESS`/`FAILED`/`COMPLETE` receipts make partial attempts visible (`src/scientist_one/research_os.py:4465-4507`). A completed receipt is not trusted on its face: status/verify independently rehydrate registry bytes, ledger/head and run identity, full ledger-to-registry closure, summary/final-event binding, and canonical research state while keeping `scientific_evidence_established=False` (`src/scientist_one/orchestrator.py:1418-1763`). Direct tests cover incomplete/failed visibility, completed rehydration, and receipt-count tampering (`tests/test_vnext_operations.py:82-201`).

## Remaining evidence reconciliation — no expanded execution authorization

- Record remaining192 Python3.11 only when its authorized execution and retained
  closure actually exist. Preserve the closed single-runtime3,294 and paired125
  scopes, exact source identities, original IDs and enclosing/nested limits.
  No historical/full-suite/scientific PASS may be inferred from their union.
- Reconcile each required row to exact completed captured IDs and scope,
  distinguishing original methods from separate wrapper outcomes, historical
  results, exclusions and timeouts. Do not expand selection or manufacture
  nested verification to fill a documentation cell.
- Keep the historical missing-sandbox and captured-launcher integration gaps
  explicit. Old suggestions to add a sandbox or new launcher E2E are unperformed
  design suggestions, not implemented coverage or authorization from this update.
- The former abrupt-process-death test suggestion is withdrawn as next-work
  direction. Actual OS-crash collection, stopped native lifetime/collector and
  execution/read-restriction work remain stopped, not pending authorized tests.
  Exception-path fixtures do not prove OS-crash behavior.
- Under D-079, report consequential findings before repair or an expensive new
  campaign; no delegated repair. Only the single reviewed private observability-
  only gates67 diagnostic, with all67 methods and unchanged7,200-second bound,
  is conditionally approved after active regression is terminal and a safe V9
  push/remote verification completes, if ready. No automatic rerun or repair
  follows; owner triage is next. This does not reopen stopped mechanisms.
- Synchronize exact closed outcomes, row-level scope and unresolved obligations
  before the final last-mutation audit. Preserve the original20-row static table,
  historical counts, adverse outcomes, external limits and absent human E4.
