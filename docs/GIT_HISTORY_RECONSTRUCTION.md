# Git history reconstruction

Status: **D083 development checkpoint published; qualified recovered-baseline
import and tag published separately; final vNext release remains incomplete**.

On September24,2026, transparent import
`4762578f023da4c172c3f3eb69c90bf99950958f` was created at actual time on additive
`codex/recovered-baseline`, sole parent `bba9ec5b652e78b5f4bfda6a101d640f0e07869e`.
Remote branch and annotated tag `v0.1.0-recovered-baseline` were fetched and
verified to resolve to that exact47-file source/test/support tree. Annotation
object: `b7d6d027b3ece23cf76930ebc0933b8f46de215b`. It is not a final release.
The published review history through component0c48e60/handoff1b1629e and main's
README bootstrap were not moved, merged or rewritten. The baseline is not an
invented ancestor of already published development commits.

One import preserves the joint recovered31-input state and eight explicitly
semantic-reconstruction modules (146 methods). Retained scoped tests are reused
at unchanged bytes; no historical364/15 recovery or fresh146 rerun is claimed.
Seven inherited whitespace warnings were retained to preserve recovered bytes.
Date-qualified baseline metadata retains its pre-publication observations;
this section and actual refs record the later operation. Private evidence,
raw payloads and generated archives are excluded, limiting public replication.

A fresh complete private preservation verified333,254 entries/213,021 regular
files/nine links with zero exclusions before import. Archive95c1e9c3 and
inventory1acf4c66 were independently rehashed; ACLs/xattrs/external link target
bytes are not attested. Exact staged and reachable-history audits covered47
files/two commits/48 blobs, final audit60c39de2; no credential, private-data,
machine-home-path, generated-artifact, large-file or specific redistribution
blocker was identified. MIT and dependency-content reviews remain scoped, not
legal certification or security/scientific approval. No force-push or visibility
change occurred. Both development branches have explicit upstream tracking.

## Preserved earlier reconstruction checkpoints

The pending/future statements below describe their dated stages. They do not
authorize moving main, repeating recovery or reconstructing published history.

The prior verified V8 checkpoint is `f6b3d92` on
`review/vnext-checkpoint`. Publication identities and observed Git status for
the accepted V9 delta are recorded in the checkpoint handoff. Qualified
recovered-baseline import/tag and final feature-history integration remain
pending; `main` stays at the owner's README-only bootstrap. V9 is a development
checkpoint, not a final release.

The earlier implementation import `ca2bca940cd22ca785d50ccca5982af4b0512ee7`
was a transparent aggregate import, not fabricated historical feature chronology.
It preserves the owner's README bootstrap and receives no baseline tag.
The exact V9 functional delta has scoped disclosure review without an identified
new blocker. Its 16 source/test files form one accepted integration boundary;
no fully verified intermediate commits or contemporaneous development history
are invented. The handoff records publication evidence only after actual
candidate/index/history checks and remote verification. Reconcile the plan
below with published ancestry without rewriting it. A candidate inventory is
not itself a measured remote tree.

On September21 the owner selected `v0.1.0-recovered-baseline` instead of the originally requested `v0.1.0-trusted-kernel`. The import will preserve the hash-recovered pre-vNext source, explicitly reconstructed tests and known limitations. It will not claim that partial resource/recovery guarantees or the safety-stopped review passed. This labeling decision does not relax public-readiness checks or current vNext requirements, and does not change historical baseline measurements.

The project was developed without Git. Imports and reconstructed capability commits will be made at their actual creation time, with legitimate configured authorship. They must not imply that Git recorded the original development process.

The user designated the existing [Scientist-Ultra repository](https://github.com/ZelinZhu-Richard/Scientist-Ultra) as the only canonical remote. A read-only [GitHub API metadata check](https://api.github.com/repos/ZelinZhu-Richard/Scientist-Ultra) on 2026-09-07 independently confirmed the exact clone URL, public visibility and default branch `main`, with reported size zero. Complete empty-history/ref verification still requires the planned stable-boundary Git checks. No substitute repository, force-push, or implicit visibility change is permitted.

On 2026-09-13 the user initialized Git and published commit `bba9ec5b652e78b5f4bfda6a101d640f0e07869e` (`first commit`). Read-only local inspection establishes that it contains only `README.md`, not implementation or run artifacts. An anonymous remote-ref check confirms that GitHub `main` has the same identity; local `main` tracks `origin/main`. This supersedes the earlier empty-remote assumption. Preserve the commit, its actual authorship and timestamp as a documentation bootstrap. It is not the verified trusted-kernel baseline or completion of vNext, and the full public-readiness audit remains pending.

## Preservation and recovered evidence

On September20, a new complete owner-only outside-repository snapshot preserved
248,246 entries, including153,005 regular files and five recorded symlinks, with
zero exclusions. Every archive member and the unchanged before/after source
inventory was verified, followed by independent final hash and permission checks.
Archive SHA-256: `84e75e359556af5c787ce6ef663a439e9a9cd820a49ccffb6d5321a06ef76634`;
inventory: `3bdbc46697d6cb28fdf8270e507bd33ab32a991f963a258ce08acb28db43f60c`;
report: `59c673cca3149a6496cd53f0d17d3023f08b4565d9b6df24788fc5deebeabbae`.
This private archive must never be published. ACLs, extended attributes and bytes
behind external symlink targets are not attested. It predates its own completion
bookkeeping, the subsequent fetch and later verification outputs.

A normal fetch and official anonymous GitHub metadata check on September20
confirmed the same public remote and README-only bootstrap ancestry. No staging,
commit, tag or push was performed. This closes preservation and remote inspection,
not baseline acceptance or public-readiness review. Older pending preservation
statements below are historical.

After the amendment/history integration reached a stable, writer-free checkpoint, the complete current tree was preserved in a separate, owner-only local archive. The snapshot has 196,946 entries, including 119,339 regular files, with no exclusions. Source stability and every archive member were checked against the inventory. ACLs and extended attributes are not attested.

- Private archive SHA-256: `3b7e20ee8022c268a233bbcfdd01040f91af9a76bdd39c35cf61fae9605a45d2`.
- Complete inventory SHA-256: `63cbfc4a8cd8d9737014b0d0da9ce3b161393e5196cd691f5367c7ab64e97eb0`.

That preservation archive contains local run state, caches, and potentially confidential material. It is not a release artifact and must not be committed or published. It predates the bookkeeping that records its successful completion.

A second complete snapshot preserves the later frozen636-test operational-reporting checkpoint and its reconciled documentation:196,986 entries,119,379 regular files, no exclusions or symlinks. Archive SHA-256 is `1cd2e59f3c7cd2705967a37ce92e185e8f739a8297af891b28517eae9e3c991c`; complete inventory SHA-256 is `79eb79a43661eb6abae11f7738cedc0be3a68fe87d5088683a682746d2d938db`. Source stability and every archive member were verified, followed by independent final hash and owner-only permission checks. The first snapshot remains intact. The second has the same local-only prohibition and ACL/xattr limitation, and necessarily predates its own completion bookkeeping and subsequent implementation.

The recorded pre-vNext demonstration package matches SHA-256 `e034f77620e210e254dfa511d9224c25cd8415ed36231739f7485159a914d753`. Its detached envelope also matches the recorded SHA-256 `73796aad3cc44735c0dfbb65a9741481823d6348cfc828e3af2b26ec9cd5d546`.

At the September 13 resumption, the temporary second-snapshot location was empty. Its successful checks remain historical evidence, not presently available backup bytes. At that time the first outside-repository archive had not yet been freshly rehashed. The subsequent September19 recovery reverified that older archive, and the complete September20 preservation above supplies the new current backup. No missing archive is treated as current recovery protection.

From that package, a separate local candidate now contains exactly:

- 26 source files, including the captured-source launcher, matching every path, size, and hash in frozen inventory `a4bc87d557696c761b0df05bf0ff3eb8dedd1a5f5d4d7fb0cf8413bff8aee786`.
- Three configuration files matching every entry in frozen inventory `95fba6687d56bd503a4689bb0c4d0ef030499af5fffe3d509e8f7d14a219a52e`.

Recovered content totals 1,142,799 bytes. Source-recovery report SHA-256: `9549daa2ef8ff654a26d6e0936d13bc7251382132523f5beb5f30c9dc337484b`. No run, custody, holdout, or release payload was extracted. The existing vNext source tree was not overwritten.

The recovered captured-source launcher also passed an isolated `--help` smoke check and refused an invocation lacking required startup isolation. All29 recovered hashes were rechecked afterward. The current Python3.14 runtime emitted a syntax warning about an existing `return` in `finally`; the frozen source was not changed. These are startup checks, not a fresh364-test baseline result.

These checks establish exact source/configuration recovery and bounded launcher operation, not a complete validated baseline, public readiness, independent custody, or scientific success.

## Unresolved baseline validation

The package does not contain the 11 historical test-source files. A bounded search of preserved project evidence and relevant temporary snapshots recovered the exact architecture validator, but not the complete original test suite. An older ten-file test tree is not acceptable: it predates the verified kernel and carries a failing 278-test report. Later vNext tests are not silently substituted for historical bytes.

The recorded original 364-test result remains historical evidence. It is not a fresh result from this recovered candidate. The original runtime-test hash is known, but a matching file has not been recovered. Support-file completeness, candidate execution, and any necessary sanitization remain unverified.

On September 19 the user explicitly authorized a documented reconstructed suite after reasonable recovery is exhausted. The renewed bounded recovery search found the exact architecture validator but not the original 11-file suite. The first outside archive was freshly rehashed successfully; the second snapshot's recorded temporary location is absent. Exact source/configuration recovery has been repeated in a new private draft without overwriting vNext. See [baseline test reconstruction](BASELINE_TEST_RECONSTRUCTION.md) for source identities, recovery limits, classifications and current executable evidence. Historical 364/364 remains separate from new reconstructed results. Baseline commits and the tag still require completed validation, a fresh complete preservation snapshot and the public-readiness audit; authorization alone does not satisfy those gates.

The current reconstructed executable suite passes146/146 on Python3.14.6 and3.11.14 against unchanged recovered source, with independent scientific/provenance review closed at its documented scope. Separate lifecycle and corrupt-output refusal scenarios also pass. This is not recovery of the original364 tests or missing architecture packet, nor security/public-release acceptance. The future import must include the reconstruction labels and limitations, not imply contemporaneous original tests.

September20 packaging prerequisite: a separate private portable candidate now
contains31 hash-matched recovered inputs, eight unchanged reconstructed test
modules, owner-authorized MIT licensing and new staging documentation/runner.
The unchanged captured launcher passes the same146 methods on Python3.14.6 and
3.11.15 from fresh physical roots. This tests the new runner, not146 additional
tests or contemporaneous historical recovery. Exact results and remaining limits
are in [baseline reconstruction](BASELINE_TEST_RECONSTRUCTION.md). The provisional
architecture packet passes four original consistency checks, retaining11 scoped
PASS/3PARTIAL/1BLOCKED dispositions. Public evidence packaging, remaining substantive
criteria, exact release review and import commits remain open. The later owner
decision authorizes the qualified recovered-baseline tag with those substantive
limitations retained; it does not change the partial or stopped criteria.

## Intended commit boundaries

Subject to verified recovery, scoped validation, preservation and public-content review, a single transparent baseline import is preferred:

`chore(import): preserve recovered pre-vnext baseline`

Retain the user's README bootstrap as this import's ancestor. Do not erase or rewrite it to manufacture an initially empty history. Any replacement of the current vNext README with verified baseline documentation must be explicit and reviewed as part of the transparent import, while the full in-progress tree remains safely preserved.

The recovered package establishes a joint executable source/configuration snapshot, not a sequence of separately verified development states. Splitting it into invented historical milestones would imply unsupported chronology or intermediate validity. If subsequent evidence justifies another coherent boundary, document that evidence here first.

Only the exact qualified recovered baseline may receive `v0.1.0-recovered-baseline`. Do not create the superseded trusted-kernel tag or present the reconstructed tests as original historical files. All existing and future vNext deltas belong on `feat/research-os-vnext`; none may be hidden in the recovered import. Reconstructed vNext commits must follow actual dependencies, include directly relevant tests, and state their import/reconstruction nature where applicable. Separate commits are for coherent capabilities, not commit-count targets.

### Preserved vNext boundaries, not invented subsystem milestones

A September21 hash reconciliation identified a complete preserved pre-correction
vNext functional state:288 inputs (73 source modules, four scripts, three
configurations, four synthetic fixtures and204 test/helper files). Its74
production/launcher hashes and153 distinct loaded test/helper hashes match both
retained2,022-test selections exactly. The private selection record is
`4559ce5363b512a837019870a436a6dea49d315f268e8761fd5dee7166f4e731`;
the preserved inventory is
`335e7a55bc28b02f65fb73a03e942923bf513c27b14b200758199b274768ddd2`.
These hashes identify private evidence, not downloadable public proof. Selected
passing workers do not establish an entirely passing historical or current suite;
original failed and unselected cases remain explicit.

The earlier intended feature sequence was a reconstruction plan, not an existing
commit list. Preserve the subsequently published V8 checkpoint in actual ancestry.
V5, V8 and V9 correction boundaries have retained preimages and identities, but
that does not establish separately passing historical intermediate states.
V9's scoped installed evidence cannot inherit the pre-correction2,022 result.
Reconcile final feature history with the qualified recovered baseline and actual
published checkpoint, retaining reconstruction labels and directly relevant tests.
Do not commit a known-failing intermediate merely to increase the commit count.

The recovered baseline has11 baseline-only paths: `RECONSTRUCTION_SUMMARY.md`,
`manifest.json`, `run_portable_candidate.py` and eight
`tests/test_reconstructed_*.py` modules. Preserve all11 in the baseline commit/tag.
The planned aggregate vNext import explicitly retires them from the feature tree,
where their frozen39-input runner and recovered-kernel interfaces do not describe
the changed system. They remain recoverable and executable from the qualified
tag; this is not a claim that vNext recreated all146 or the missing364 original
tests. Current regression tests and [baseline reconstruction documentation](BASELINE_TEST_RECONSTRUCTION.md)
are the respective execution and provenance successors. Any index deletion must
be exact and individually reviewed; no broad staging or deletion of current
private files is authorized. This disposition has not yet been applied.

## Public-release and completion gates

Conservative ignore defaults and the [public-release policy](PUBLIC_RELEASE_POLICY.md) remain in force. The published V8 checkpoint has its recorded scoped review. V9 functional-delta disclosure review reuses unchanged evidence and identifies no new blocker at that scope, but synchronized claims, final candidate hashes/modes, effective ignores, actual index/tree, reachable history and publication refs still require review. Private originals remain excluded and preserved. Software-source publication is not scientific or final-release acceptance.

The original project audit rejected a synthetic compressed Git object as invalid text. A Git-aware adapter and captured audit-publication integration have since passed a frozen 135-test affected matrix and independent scoped review. They retain raw Git metadata in the snapshot and verify supported decoded objects/history before granting exact binary exceptions; `.git` is not blanket-excluded. See [Git-audit scope and limitations](GIT_AUDIT_BOUNDARY.md). These were isolated synthetic Git fixtures, not project history or a public-readiness audit. Actual project refs, staged content, reachable history and final snapshot freshness still require verification after initialization.

No project content may be pushed until the exact proposed public files and all reachable commit history pass secret, confidentiality, private-literature/data, machine-path, generated-artifact, large-file, dependency-license, copyright/redistribution, effective-ignore, and README-claim review. Uncertain content is excluded while the repository is public. Ignoring a file after it entered history is not remediation.

Staging must be selective. Each commit requires inspection of staged filenames, the complete staged diff and statistics, content scans, and targeted tests of the actual candidate. No commits occur while subagents may be writing.

After clean audits, publish verified `main`, then the baseline tag, then the feature branch, fetching and checking exact refs before each push and configuring upstream tracking explicitly. Do not merge unfinished vNext into `main`. Completed vNext requires a reviewable pull request or documented nonsquashed merge after full validation.

Final reporting must include actual commit identities and boundaries, per-capability tests, combined nonoverlapping totals, untested paths, scan outcomes, ignored-file policy, pushed refs, tracking, final status and graph, `main` contents, and remaining public-release risks. Git operations must precede the final project audit; the adapter's scoped tests do not substitute for checking the actual final repository and preserving its last-audit freshness guarantee.
