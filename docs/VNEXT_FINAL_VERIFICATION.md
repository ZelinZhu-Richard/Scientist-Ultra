# Research OS vNext Final Verification

Status: **D083 OFFLINE PREPARATION INTEGRATED; FINAL VERIFICATION INCOMPLETE**
Capability ceiling: **`AUTONOMOUS_EXPLORATION_READY`**

Current305-input synthetic lifecycle is now technically verified on Python3.14.6.
Source freeze `8204cca1` preserves all prior303 bytes plus only the Colab source/
test addition. VNext fixture/status/verify has closure `ddf4f878`, report
`52e14228`:342 artifacts/67 events, `MORE_EXPERIMENTS_REQUIRED`, no scientific
writing/release approval. Command times were1107.06,46.07 and45.93seconds,
within unchanged7200/600/600 bounds. Legacy demo/verify/resume/verify/reproduce/
verify/package/verify has closure `a236376b`, report `48db98f3`:75 artifacts/
22 events,238 private package files. Reproduction returned the expected legacy
exit1/`ARCHITECTURE_CONTROL_REPLAY_PASS`, difference0 at tolerance1e-12.
Resume skipped an already complete run; this is not an actual interruption/crash
test. Demo already generated the same reproduction/package receipts, so later
readbacks are not additional experiments. Root rejoined all raw outputs, exact
commands,305 current inputs,85 lifecycle before/after entries and unchanged
original receipt bytes. This does not authenticate a new host/user or confer
live, scientific, E4 or public-release authority. Architecture remains provisional;
qualified history is published, four structural checks pass both runtimes, and
final audit remains pending.

September24 current reconciliation: original192 modules/3294 IDs pass on
Python3.11.15 at D080 source303 `8f5b3669`, closure `68f0a148`, result `395aeae1`,
885 retained files. Operations18 is separately paired; focused numerical/CLI24
is separately paired. These are enclosing-only records, not additive private or
historical counts and not a paired V9/D0803294 result. The subsequent Colab
two-file extension has15 focused tests on both runtimes, results `6374701c` and
`309477c7`, unchanged298 captured Python inputs and matching15 IDs; root checked
raw/worker/loaded-source joins. Source `f154a439`, tests `4ca1e3f5`, non-author
review `58b83207`. No live/worker/CU/scientific authority is established. Prior
full-source captures remain at their recorded identities; current synthetic
lifecycle is closed above; whole-architecture acceptance and final audit remain pending. See the
[current handoff](../OVERNIGHT_REPORT.md#current-handoff--owner-d083-september-24-2026).

The hash-bound architecture report061277c0 and ADR1818a4e2 were frozen before
the structural checks and qualified-baseline publication. Their prospective
“when run” and pending-history statements describe that earlier preparation
boundary. The later checks/publication are recorded here without rewriting
those bytes or retroactively promoting the report's5 scopedPASS/8PARTIAL/
1BLOCKED/1PENDING dispositions.

The ceiling remains provisional, based on earlier bounded synthetic fixtures,
not a fresh-user deployment claim. The owner-approved permutation correction
has 18 focused installed tests passing on both Python 3.14.6 and 3.11.15,
after a private candidate passed the same selection and non-author review.
These are the same IDs across runtimes/copies, not additive test counts.
Independent Fraction means verify comparison semantics. The preimage run
retains 8 failures and 1 error across 18 tests (one failure deliberately expects
the new method label); the scaled example specifically returned 1.0, not 1/35.
Source SHA-256: `d19f8cf87b08cf79c30d005f91d5745c6ca874216b7b73848d8aa38fa31ec420`.
New test SHA-256: `55f30b1c25897ce0a3688512f127d4e5739fe2a6190592f04464c919e0c70ab9`.
Installation removed only an extra final blank line from the reviewed test
candidate; the complete source patch and test behavior are unchanged.

No historical result is migrated. Only `analyze_two_group` directly calls this
helper in repository source; its method description now identifies comparison
v2. Original full-source V9 attestations below do not validate the changed tree.
The safety-stopped scientific-core module was not rerun or bypassed; focused
numerical tests establish no authority/protocol/security or real-study result.

The single approved gates67 phase diagnostic is now terminal TIMEOUT at
7200.020898582879 monotonic seconds. Its 106 markers include 52 closed spans,
but no complete captured worker payload, so individual outcomes remain UNKNOWN.
Nested times overlap. Source-level output retention occurs after return/timeout,
not as live durable progress; cleanup and root cause remain unknown. Original
and interim evidence is retained. Further investigation/repair/retry requires
owner triage. The conservative CLI packaging correction removes the unsupported
console declaration without modifying cli.py or the captured launcher. Six new
fresh-source checks pass on both runtimes; the combined focused selection is
24 distinct tests, not an additional count on top of the numerical18. Help
works without private state; status/start with a supplied question refuse before
run creation because fresh app-bound admission is unavailable. Legacy status
mapping is checked read-only, not by fabricating launch authority. The real
question-driven workflow and final verification remain incomplete.

## Preserved V9 exact-source evidence

September 23, 2026 checkpoint, incorporating the Python 3.14 terminal observation at 16:19:20 UTC: V9 is installed and frozen with 300 functional inputs.
The same 125 selected IDs pass on Python 3.14.6 and 3.11.15 at enclosing scope;
a disjoint 3,294-ID selection passes on Python 3.14.6 only. This is 3,419 distinct
selected current IDs on Python 3.14, with only 125 currently paired; remaining192
on Python 3.11 is NOT_STARTED. Nested/helper behavior remains NOT_ASSESSED by
the enclosing closures. These are not whole-suite, security, scientific or
release acceptance.

V9 is installed and its 300 functional inputs are frozen: the independently
reviewed 16-file union comprises nine replacements and seven additions, with
284 unchanged neighbors. Installation proof SHA-256:
`58c469e5796559217a3877f732cd2d56530d7c027f4994a46093c507f0277976`.

The same 125 distinct selected IDs pass on Python 3.14.6 and 3.11.15, with
enclosing closure
`80ab70ebc68277032e797b40e108f025d12a2493b8eb7e7bb42056e8bf8b0943`.
The separate 192-module selection is terminal with exit 0 and 3,294 successes
on Python 3.14.6 only; its single-runtime enclosing closure is
`1475c93cce4b0c6ee519cfb357ba49740bceeb3f74d8b479d79b9efcf2911955`.
These disjoint selections cover 3,419 distinct current IDs on Python 3.14;
only the 125-ID selection is currently paired. The remaining192 Python 3.11
selection is NOT_STARTED. Both closures leave nested/helper behavior
NOT_ASSESSED; none of these counts is a full-suite, historical, scientific,
security or release PASS.

The Standard source scan is sealed PARTIAL, with 43 review products and three
LOW findings. Accepted corrections are installed and have scoped test evidence;
this is not full security PASS or clearance of omitted/stopped scopes.

Gates67 retains its original paired and diagnostic 7,200-second timeouts
without complete outcomes; isolation38 retains 37 successes and one inner
900-second timeout error per runtime. Causes remain UNKNOWN. The retained
lint33 result is non-PASS. Original failures and separate private checks remain
preserved, not relabeled or added to installed counts. Operations18, fresh
installed lifecycle/reproduction/package, the current architecture packet and
its four validator methods, and the final audit remain pending. Architecture
packet validation is not proof that all 15 behavioral criteria pass.

The [current handoff](../OVERNIGHT_REPORT.md#development--review-checkpoint-september-23-2026)
retains exact safety exclusions, D-079 owner triage and the conditional single
gates diagnostic approval, plus the actual V9 publication identity and observed
Git status. The prior verified V8 checkpoint is `f6b3d92`. Qualified
baseline/tag/history and final release verification remain pending; a safe
development-source push does not close them.

## Earlier verification checkpoints — historical

The following dated descriptions retain original identities and outcomes.
Their “current,” “running,” “uninstalled” and Git wording refers to those dates,
not V9. They do not authorize repeats or contribute an additive final-suite total.

September21 V8 verification checkpoint: the reviewed 15-file correction union is
installed and its 293 functional inputs are pinned by installation proof
`8a3f19fe7ad41f39b534111623ddd435775f6b0cc5d190e128d8445a747383a9`.
Six whole modules (207 distinct tests) pass on both Python3.14.6/3.11.15 with
root-verified enclosing evidence `34382e74b5e6128f53ecdbdd5f286e2330d071fbe461dea119596cb56523e404`.
The combined468 selection now passes all17 whole modules on both runtimes.
Root retained-data verification
`807533669934d579221207934682cc75378466e4afea61cae93343004729234f`
closes exact468 original IDs, all293 functional inputs,287 captured Python inputs,
source/loaded attestations and the retained nested populations:282 resource/
observed captures,197 admission captures and33 compute original/wrapper pairs
per runtime. Those populations are not extra test IDs. Canonical4, two resource-
boundary methods and17 compute/exposure cases retain enclosing-only limitations;
the intact original second-reserve25 remains unselected/unreproduced. This is not
full-suite, scientific or release acceptance. Root closure
`8cf5ec2596082439b105ea0c5eebab7fbda8b8535816293526f6d21f8404dad9`
records the exact limits. The separate168-module captures are now terminal:
each actual runtime recorded2,601 successes and1 error across2,602 distinct IDs.
Both strict parent harnesses correctly returned1. Root retained-data closure
`376132b6b658b8e81efa7e404c06cfd38290cb3b3a54550fe4db6980751844c9`
joins the complete raw/worker/enclosing reports, original IDs, source/loaded
attestations and unchanged293 functional inputs. This is enclosing-only evidence,
not a green2,602-test result. The error occurs before the post-result terminal-stop
assertions: an obsolete test-token command is rejected by the built-in provenance
predicate. Production guards remain unchanged.

A reviewed private test-only terminal fixture correction passes the whole three
original methods and eight separately counted new helper controls on both runtimes.
Root private closure
`10e74fdcafe28ade9aa200f3771d01c99d97dca2c8fd415b434c237b66e7aeb7`
binds the two independent copied roots, exact method outcomes and source closure.
It uses explicitly synthetic NEW_TEST_FIXTURE bootstrap receipts, not app/human/
scientific authority. This is not installed acceptance, and neither original
failed batch is relabeled. The separate CLI68 token-fixture dependency is being
corrected privately; no CLI68 result is claimed.
Isolation38 completed with37PASS/1ERROR per runtime:
the inner guarded fixture timed out at900 seconds. The separate operations18
owned-temporary-namespace retry also timed out at7200 seconds on both runtimes,
with no captured operations outcomes. Complete failed/timeout evidence is
preserved by `3dfe78cbd44361475ae3556b86ebb7d6d1caa2b52a711c67476e38800846cf72`;
the two parent captures are terminal, not18FAIL orPASS. Independently reviewed
private isolation38 timeout-retention and operations18 lifecycle diagnostics now
run on Python3.14 with unchanged whole-module selection and time limits. They
are informed diagnostic attempts, not installed acceptance or presumed success.
The earlier gates67 paired7200-second timeouts retain unknown
outcomes/cause. The reviewed private whole67 diagnostic also timed out on
Python3.14.6, with54 complete valid progress markers but no captured outcomes.
Root preservation `a8f340e5e51871280e5b9f5341ed29b0ca87b79a9f263722ece8f56b5e27f08c`
joins293 installed,294 private and287 captured Python inputs. The last retained
marker is CASE_SETUP END for the owner-backed paper-failure case. It is not proof
of an active test body, successful prior tests, a root cause or historical timing
equivalence. Three immediate owned temporary directories remain; no cleanup was
attempted or verified. No temporary payload or descendant inspection occurred.
Neither running private diagnostic changes installed inputs or the original
deadlines. No additional gates retry or budget increase is authorized here.
All installed Python inputs remain frozen. These observations supersede older
running/candidate descriptions below, not their preserved failures or scope limits.
Historical364, reconstructed146 and overlapping private results remain separate.
The qualified `v0.1.0-recovered-baseline` tag is authorized, but no implementation
import, tag or push has occurred. Final verification and public/Git gates remain.

Separate installed Local Mac constructor verification is complete on both
Python3.14.6 and3.11.15: three unit successes and one disabled real-host skip,
not4PASS. The unchanged whole-module capture returned0; the strict parent harness
returned1 because its all-green condition rejects skips. Root verified exact
IDs, raw reports, loaded/source attestations and unchanged inputs; closure
`13877a5e111f01e7782d91bb76a2e2ab3a3b54a0379d488d6470b21fc3965e92`.
The real-host probe was explicitly disabled; this supplies no sandbox or
scientific-isolation acceptance and stays outside any all-green total.

Earlier September21 pre-V8 checkpoint: the installed affected393 batch is not green
(392PASS/1FAIL on3.14.6;391PASS/2FAIL on3.11.15). Its complete failed enclosing
evidence is preserved and verified; operational PILOT-pair compatibility is being
corrected in a private candidate. Installed offline326 has verified enclosing
closure on both runtimes. Separate private observation165, three-output failure131
and GPU routing5 pass both, with retained nested evidence checked at their stated
scopes; none is an additive installed total or final acceptance. Further40+165
fixture regressions are running. Original second-reserve missing-source and late
source-mutation coverage remains unresolved under current capture guarantees.
The owner-authorized baseline tag is now `v0.1.0-recovered-baseline`, with partial
and stopped historical criteria retained; public-readiness and Git work remain.

Historical pre-integration September20 selected evidence: **2,022 distinct IDs pass on
each actual Python3.14.6/3.11.15**, across retained hash-reconciled captured runs,
not one full-suite invocation. Exact report
`.scientist-one-build/goal-evidence/20260920-fars/current-selected-2022.json`,
SHA256 `4559ce5363b512a837019870a436a6dea49d315f268e8761fd5dee7166f4e731`.
Both runtime counterparts of E2E8, paper-edge3 and ablation-state3 are now complete
and included. Runtime/recovery100, calibration34, amendments25 and state56 are
also included. The eight synthetic GPU-lifecycle controls pass a bounded retry
using separate module temporary parents; original timeouts retain unknown cause.
This is no GPU-execution claim. Two state namespace child tests use a separate Anaconda Python
path; their parent-runtime report does not establish an identical child version.
The exact reviewed21-file integration is now installed, including admission/PILOT
durability and prepared fixtures. Corrected PILOT/CONFIRM120 passes both runtimes
in private copies with127 independently verified raw captures each; its preceding
119-case attempt remains116PASS/3FAIL. Charge34 plus68 dependent controls and
observer8 have separate scoped private closure. Installed affected-regression
runs are in progress, not acceptance: the2,022 selection above predates these
source changes. Two new lint style findings remain for the next edit boundary.
Observation63 and related26 contract fixtures require
complete prepared captured roots before execution; source guards remain intact.
Actual OS-crash collection remains open, distinct from synthetic exceptions.
The dated earlier counts/running-state descriptions below are historical, not
current totals. Historical364, reconstructed146 and private/support evidence are
not added to this selection. No scientific, stopped-review or release gate closes
because of this selected-regression total.

The unrestricted whole-suite command must not be used while its selection
includes exact safety-stopped probes. Mandatory full regression remains an open
requirement, not a waived or silently passed one: execute all permissible whole
modules through the existing captured interface and report mixed stopped modules,
fixture prerequisites, timeouts and unavailable platform reviews separately.
Do not add a selector, suppress tests or reroute a stopped review to manufacture
an all-green total. Git/public-readiness and final-audit gates remain independent.

September 20 bounded FARS addition: [the source-backed assessment](FARS_DESIGN_REVIEW.md)
adopts only A01 operational output-completeness enforcement. Corrected14 tests plus
39 unchanged compatibility methods pass on Python3.14.6/3.11.15 under exact source
and loaded-test identities; no single green53 invocation or final whole-suite
result is claimed. The review also preserves existing contract/plan/controller,
claim-support and deterministic composition owners. Optional advisory-note
indexing and portfolio/DAG additions are deferred; gate-bypassing fallback,
positive-result-only progression and another provider proxy are rejected.
Real FARS implementation behavior, comparative performance and our live scientific
deployment remain unverified. The current specification update changes routing
prospectively, not the historical baseline, completion scope or unresolved gates.

This report is the final evidence contract for the vNext goal. Until every pre-audit `PENDING` field below is replaced by a measured value or an explicit unresolved disposition and the final audit is run after all other audited mutations, it is a verification plan rather than completion evidence. An unresolved disposition is not PASS or Goal completion. The final-audit row is deliberately resolved from `reports/final_audit.json` after this document is frozen; it is reported externally rather than backfilled here. The preserved pre-vNext record in `baseline_before_vnext.md` is historical evidence, not current verification of either the reconstructed baseline or changed vNext source tree.

## What is implemented

Scientist-One vNext preserves the existing trusted kernel and adds a provider-independent research-intelligence plane over the same `ArtifactRegistry` and `EventLedger`. The implemented plane includes controlled external acquisition, one OpenAI Responses reference provider, replaceable scholarly adapters, passage-level reference verification, structured problem/novelty/hypothesis/evaluation design, discovery history, real local child execution, provider-neutral compute contracts, six domain-validity adapters (the four required domains plus Time Series and Recommender Systems), independent human/scientific gates, Challenger and soundness review, receipt-bound claim evidence-use, canonical research state, and an evidence-first paper/venue verifier.

The scientific-source lane additionally implements: one-shot, run-bound HMAC authority for an exact built-in live HTTPS crossing; prospective source/license/use-reviewed and atomically published `Dataset` plus four-role `Split` custody; zero-write-preflight Result-v2 bundles with an optional later exact `StatisticalTest` and current bundle-completion authority; canonical outcome-independent ClaimSemantics proposals; the acyclic citation order `E -> G -> P -> Jref -> K`; exact `Jqual` authority for scope and limitation claims; and source-owned v2 Ablation authority. Synthetic reproduction is categorically non-scientific and cannot satisfy scientific robustness.

The supported integrated run is deliberately a synthetic system fixture. It may verify plumbing, provenance, deterministic validation, failure handling, and complete read-back. It cannot establish real-world novelty, scientific effect, generalization, publication readiness, independent custody/review, E4, or superiority over `UPSTREAM_SCIENTISTONE`.

## Final measured evidence

### Current D083 checkpoint — September24, 2026

These values supersede the dated table below, without changing historical
measurements. Raw evidence, bootstrap material and generated packages remain
private. Public hashes are scoped evidence locators, not downloadable independent
replication or host/scientific approval.

| Evidence | Current measured value / limitation |
| --- | --- |
| Current functional freeze | 305 inputs; proof `8204cca17d76b7de2a7960b79b8ec8deff287eeb566d6d75aad0badbde5d0e1b`; all303 predecessor inputs unchanged, only Colab source/test added |
| Prior303 selected regression | 192 modules /3,294 distinct IDs on Python3.11.15, zero bad outcomes; result `395aeae17a4777313b2dfa01a2a1f95fe7711821abb4101512e7860464dc5783`, closure `68f0a148cd8f623f9f12d53477cbe04dae64db525536ee1f111b38446acaa749`; not paired with older V9 Python3.14 or a full current305 suite |
| Separate affected checks | Same15 Colab IDs pass3.11.15/3.14.6; same18 operations IDs pass both; focused24 includes numerical18 plus CLI6. Do not add runtime repetitions or overlapping historical selections |
| Whole-suite acceptance | UNRESOLVED: exact stops, gates67TIMEOUT, isolation38 timeout error and lint33 non-PASS remain; enclosing evidence is not nested/helper verification |
| Architecture | Pre-audit provisional report `061277c090e8fe989c6e8dcf49a0b23f59a9c210befdb33ec30394c955f6c93d`, non-author review `64020b29fd234ddbcf23b13888b38574431a99bf504961f3de5194bd0d1be187`:5 scopedPASS/8PARTIAL/1BLOCKED/1PENDING. Same four structural methods pass3.11.15/3.14.6, results `510c1181b280b813caec268a097dad58d0c35859f60788fcdb78e964723ca69c` / `093445beb4362ad33f241fb0c238bdbe6fd948a7b104e375f0b6351be6404d4d`;298 captured inputs and raw/worker bindings rechecked. Not15 behavioral passes |
| Syntax compilation | All78 current source/launcher files compile on both runtimes; input-map `4aa12488cddb6e36e2c71f2c327f1b94f95333427ebf0ae8d4410b440db06540`; no subject import/execution or bytecode files |
| Current vNext run | `vnext-final-current-20260924T042350Z-ac8bcf3968d00c17`; three guarded commands, Python3.14.6, technicalPASS; closure `ddf4f8781b6a0da699f3e11ed33ef224d76c43500ae33540d90326c3ebada118` |
| Operation and registry | COMPLETE; operation file `7bb76c202e931824aeb10c09a4cb23082ae3365bd098ad257dd0762d30db0b25`;342 records, registry/ledger/receipt binding verified |
| VNext ledger | 67 events; head `57c07145914f1b6e9b8f4dcf2de4bf970d33c144393d2144a06c224784dde8f2`; file `cb6eef867c6adf0182d7361958a8b3b039cc848e03cad43b0ff9f20325e80a2f` |
| Canonical state | 40 objects/21 types; final snapshot `e06e27988305914be7a2b916b2b27e532d7c93381e71d6d4c0e50a7c46254278`; existing guarded verifier confirms binding |
| Integrated summary | `0a5802c1f74f515ef16713516df80d4b22718821f4cc33b74386762561a18eec`; scientific MORE_EXPERIMENTS_REQUIRED, writer false, paper BLOCKED, venue NOT_READY, no E4 |
| Current legacy run | `run-20260924T044518Z-166217a8bd`; eight-command expected exit contract; READY_FOR_HUMAN_REVIEW/COMPLETE_DEMO_ONLY;75 artifacts/22 events; manifest `e92402b6d3cc14c22cb2cc600afe0a78642c107fb8025769b6141c9c3465c0f7`; closure `a236376b7a2e64638d0b6c919ecdfef0e2f94fc8ab523019e705da0407635b69` |
| Synthetic reproduction | Exact1.0 vs1.0, difference0, tolerance1e-12; expected CLI exit1/ARCHITECTURE_CONTROL_REPLAY_PASS; manifest `957cfba5edf5a99e6abbd6cdfbb671de1565b9db4430a2bcb731fef0cd0baec3`, result `cb78bf96eb409f609aa238f952f0a1476ac01e0e8343ac3d47d8d7e9b0779366` |
| Private legacy package | 238 files; ZIP `8cb4cb087225fd004b0de35bf71d332f2f6c44026c4109183829a10582329f32`, envelope `6ad2f9661be3cb6fd2ae1d0beca93b840c9faf876ade7185e660c295208aa96c`; byte-hashed and post-package verificationPASS; never public, E4 required |
| Recovery/repeated commands | Resume SKIP_COMPLETED/zero replay events. Demo already created the same reproduction/package receipts; later readbacks are not additional studies or actual OS-crash recovery |
| Qualified history | `codex/recovered-baseline` and `v0.1.0-recovered-baseline` resolve remotely to `4762578f023da4c172c3f3eb69c90bf99950958f`;47 reviewed files,31 recovered inputs,146 reconstructed methods. Main unchanged |
| Final audit | PENDING last mutation; its result must be reported from `reports/final_audit.json` externally, not anticipated as PASS |

AUDIT-001 is awaiting owner triage: current `git_audit.py::_classify` excludes
app-managed ref namespaces present in the repository. This predicts fail-closed
metadata refusal, not an exploit or completed runtime failure. No deletion of
app history, audit exemption, production change or repair campaign is authorized.
The already-required audit will retain its actual outcome. This whole-project
check is distinct from the scoped outgoing-content checks used for development
publication. The pre-audit architecture report predates the later qualified tag;
its pending final-packet row is not retroactively promoted by publication.

### Historical pre-D083 measured table

The following original values and pending statements describe the earlier
checkpoint only. They neither replace the current table nor authorize reruns.

| Evidence | Final value |
|---|---|
| Frozen post-vNext captured suite | `PENDING` |
| Captured test report SHA-256 | `PENDING` |
| Frozen V9 functional inputs | 300-input installation proof `58c469e5796559217a3877f732cd2d56530d7c027f4994a46093c507f0277976`; final post-verification/mutation equality pending |
| Current selected runtime evidence | 125 distinct IDs paired on Python 3.14.6/3.11.15; disjoint 3,294 IDs pass on 3.14.6 only; 3,419 selected current IDs on 3.14, only 125 currently paired; enclosing-only limits above |
| Standard source scan | Sealed PARTIAL, 43 products / three LOW findings; accepted corrections installed with scoped tests, not full security PASS |
| Mandatory architecture controls | `PENDING` |
| Architecture report SHA-256 and targeted validator | `PENDING` |
| Final vNext run ID | Pre-V8 post-FARS guarded fixture `vnext-final-20260920-fars` completed; a fresh final-source run remains pending |
| VNext operation receipt status/SHA-256 | `COMPLETE` / `f82056a53b56459f0c3e026e9e19bb5645168b8841f78c50f45d3c763fd52162`; normal status and separate deep verification both `PASS` |
| VNext registry count/validation and closure | 343 / `PASS`; separate deep verifier confirms receipt binding and ledger closure |
| VNext ledger count/head/file SHA-256 | 67; head `52fcc0d124de36f18441fcb344da11e4e1530329ecb8e0da15378614261cc0a8`; file `be75ffaa99b5d0fc465ffd7249fab61ef5115dd261f0cc8976be781e3d6bb071` |
| Canonical object count/type coverage/final snapshot SHA-256 | 40 objects /21 types; separate deep verification passes; final snapshot `dfb5e70b15b32ebfbb358dce6a025efae5cf41930a7ed1a03a188c507d4d2eff`, freshly rehashed from registered bytes |
| Integrated summary SHA-256 | `f286924632ae3d33a19fc5166b2dda026a933b9e515af064805ed4c60f8deaeb` |
| Final legacy demo run/state/outcome | Pre-V8 `run-20260920T174324Z-5cf9a4d002`; `READY_FOR_HUMAN_REVIEW` / `COMPLETE_DEMO_ONLY`; separate seven-command ladder passes its expected outcomes; fresh final-source ladder pending |
| Legacy registry/event counts and manifest SHA-256 | 75 artifacts /22 events; manifest `3d7604780adfe55da992f2677321058835f3fe482216b585e1b0d87fa819f17c` |
| Legacy reproduction manifest/result | `ARCHITECTURE_CONTROL_REPLAY_PASS`, difference0; manifest `451f81ff39ab4e6695054bc6f90d42b10a333c2a25fc2637bd9f4f0bd305ccd6`; result `b434057d74805cb883c61373caab46ae39a8ad6ede801732f260c721e61e18b0` |
| Legacy package ZIP/envelope | Same237-member demo-only archive on repeated packaging: `d854c058c9c3b542563ef4022bb5bbc976c861efa55aaee4b09c7dc7c3297b87`; envelope `6a1a348128e67a9f9f635457d6acd1a0c9f71ce3d398fc4a1d56222bb8a3ea26`; post-package verifyPASS, E4 still required |
| Final project audit result/snapshot/report SHA-256 | `PENDING`; after the last mutation, read from `reports/final_audit.json` and report externally rather than editing this file |

## Before and after

The post-FARS selection additionally passes40 domain-adapter and15 governance
methods on each of Python3.14.6/3.11.15. A separately reviewed test-only correction
replaces an obsolete injected plan-custody runner with the existing bound child
interface; its three unchanged test methods pass on both runtimes. Earlier setup
errors remain recorded. These selections are not the full captured suite, real
domain experiments, an independent custodian, or a substitute for a stopped review.

Another six explicitly selected contract/reporting modules pass47 distinct
captured methods on both runtimes. Result hashes are
`d62584637fcec322f25a7609efd0e5a0dd5c5794ebf2340fdac81c6cfdfdaef7`
and `b0128ede25df64a17dd164329161cb5f06ce75eb41a773a64b2b99015f444d14`.
All281 inputs, worker logs and loaded test-module closures were freshly rehashed;
all bad outcomes are zero. Runtime duplication does not double the distinct test
count. These selected checks do not complete the frozen full-suite requirement.

The later claim/manuscript/replay selection contains137 methods, not the initial
static estimate147. All passed on Python3.14.6; Python3.11.15 exposed five
AST-format golden failures and one frozen-slot exception mismatch. A reviewed
two-test-file portability correction preserves every existing golden and
declared-field immutability assertion. Its18 methods pass both runtimes;120
unaffected loaded closures retain passing evidence, giving138 distinct passing
IDs across retained/corrected runs, not one green138 invocation. No production
code changed. The next125-method selection has123 passes and two setup errors
on each runtime: a temporary I0 fixture's inert source tree does not match the
guarded launcher's captured source identity. That guard remains unchanged.

An earlier selected evidence checkpoint contains 1,340 distinct passing test
IDs on each actual Python 3.14.6 and 3.11.15 runtime, reconciled from retained
captured reports rather than one full-suite invocation. The earlier 997 checkpoint
is retained; subsequent disjoint batches add 82 domain/admission/metadata,
48 prospective GPU/method, 34 checked-result/external-validity, 58 cohort, and
121 numeric-ablation checks. Selection report:
`.scientist-one-build/goal-evidence/20260920-fars/current-selected-1340.json`,
SHA-256 `dfc562c922f9596bd3eb9a3497dd7b938d68442323a7aa5f27b7cb1749a8dca4`.
The ten-method snapshot module remains excluded for one Python 3.11 AST-dump
portability failure; its reviewed correction is uninstalled and unrun.
Resource4 remains installed at v3 with two passes/two failures per runtime;
private v4 passes four/four but is not installed. E2E8 passes on Python 3.14.6
(result `c9816a6594104048ae37eced99848374a85f3e0dc5f3de85a0cf36900370ad56`),
and paper-semantic-edge3 passes on Python 3.11.15
(`3443d671b86873ecb5e082b37559f02287d154b6cdae04c2e030ff542547eaed`).
Their other-runtime counterparts remain running, so neither batch is included
in the paired 1,340 count. Additional selected regressions remain in progress.
The four-method I0/envelope module remains excluded: its original two setup
errors were followed by a test-only prepared-root correction whose two child
scenarios exposed one missing pinned synthetic calibration fixture. A reviewed
private physical-copy correction now passes4/4 on both runtimes, but installed
acceptance is still pending. No production guard or assertion was relaxed.
The initial8-method E2E selections timed out at900 seconds without a captured
worker result; no partial pass is claimed. A prior successful normal fixture's
1034-second duration justified a separate7200-second bounded harness. The first
E2E and paper-edge results above took 3717.267 and 2805.928 seconds respectively;
the cross-runtime checks remain pending, with the original timeouts preserved.
These are not a single full-suite pass and must not be added to
historical364, reconstructed146/4, or older PMC/provider423 evidence. Final
captured-suite, safety, architecture and public-release gates remain unfinished.

| Guarantee or capability | Historical/pre-vNext record (not current verification) | Post-vNext status |
|---|---|---|
| Filesystem/path/serialization/command security | Preserved historical record | `PENDING_FINAL_REGRESSION`; stopped reviews are not cleared |
| Content-addressed artifacts and ledger provenance | Preserved historical record | Extended to canonical research state and vNext phase/experiment materialization; complete regression remains pending |
| Protocol, confirmation, holdout, recovery, reproduction, packaging, E4 | Preserved historical record | Current-source synthetic legacy lifecycle/read-back ladder closed at the exact scope above; real custody, scientific confirmation, E4 and full regression remain separate unresolved gates |
| Claim-evidence authority | 12-kind graph with support receipts | Evidence-use class and independent resolver receipts added; final tests `PENDING` |
| Scientific state promotion | Legacy fixture state | Source-owned Result-v2/optional `StatisticalTest` bundle completion, current-revision replay, Dataset/Split authority, exact qualifier authority, and v2 Ablation replay added; final captured regression `PENDING` |
| Citation/semantic authority | Legacy citation support path | Canonical ClaimSemantics plus acyclic `E -> G -> P -> Jref -> K`; exact signed-transport and judgment custody; final captured regression `PENDING` |
| Controlled external/provider/literature plane | Absent | Implemented and fixture-tested; live validation `BLOCKED_EXTERNAL` |
| Structured problem/design/discovery/experiment plane | Absent | Implemented with synthetic integrated exercise; scientific validation unestablished |
| Domain validity | Legacy bounded calibration | Four required typed adapters implemented; selected40 domain methods plus15 governance methods pass both runtimes; no real domain study or full-suite clearance |
| Soundness, paper, and venue gating | Legacy review packet | Independent typed layers implemented; fixture correctly blocks promotion |

No row may be promoted from `PENDING_FINAL_REGRESSION` until the exact final evidence table above is complete.

## Compute status

| Mode | Implemented | Locally tested | Scientific/external status |
|---|---|---|---|
| `LOCAL_MAC` CPU | Frozen specs, adaptive plans, bounded execution, checkpoints/recovery, manifests, registry return, clean rerun | Technically exercised during development; frozen suite/run identity `PENDING` | `BLOCKED_LOCAL`: no OS-enforced filesystem/process/network sandbox, so no current Result can acquire scientific backend-execution authority |
| `LOCAL_MAC` MPS | Typed profile and validation boundary | `PENDING_FINAL_SUITE`; no physical vNext MPS run expected | `UNTESTED` unless a real validated run is recorded |
| `GPU_CLOUD` | Provider-neutral profiles, escalation, scheduler lifecycle, checkpoints/preemption/queues/artifact return boundary | `PENDING_FINAL_SUITE` with fixtures only | Real CUDA/scheduler/hardware/credential validation `BLOCKED_EXTERNAL` |

The D070 operational terminal adds a separate, narrow boundary: actual prospective contract/plans, retained CUDA-device timing inputs, design freeze and escalation ownership can establish one mandatory unperformed measurement outside admitted LOCAL_MAC CPU/MPS capabilities. The resulting compute-family assessment and canonical `FULL_VALIDATION_REQUIRES_GPU_CLOUD` are non-scientific operational records with no spending authorization. Root414/414 affected tests passed on2026-09-06, and independent integration review closed after the genuinely reproduced wall/GPU publication-order repair. Its real routing controls passed independently5/5. This is not final frozen evidence, actual CUDA compilation/execution, binary equivalence, cloud availability, scientific adequacy, or proof that all study requirements are complete. Exact component evidence and earlier failures are in `.run/STATE.json`.

## Domain status

| Domain | Implementation | Verification status |
|---|---|---|
| Generic ML | Meaningful split, leakage, benchmark, training-policy, resource, seed, metric, robustness, and generalization checks; integrated into the fixture | `PENDING_FINAL_SUITE`; no real scientific workload |
| Medical Imaging | Patient/session/site/modality/preprocessing/task/external-validation/uncertainty/sensitive-data validity logic | `PENDING_FINAL_SUITE`; no clinical or sensitive dataset used |
| Operations Research | Feasibility, objective/constraint, baseline/bound, timeout, paired-instance, solver/hardware, scalability and fairness logic | `PENDING_FINAL_SUITE`; no real solver study |
| Systems | Hardware/software/workload/warmup/repetition/noise/tail/memory/energy/isolation/concurrency and proxy-scope logic | `PENDING_FINAL_SUITE`; no real systems benchmark |

## Provider and literature status

Current D071/D074 reconciliation: this section describes component contracts, not one complete live provider or six complete live scholarly routes. OpenAlex has a native keyless route. PMC framing, supplied coordination, outcome composition and versioned wire/custody replay are installed and offline-tested, while production activation and genuine native/TLS/live/signing deployment remain closed; the other four listed adapters normalize supplied source-shaped payloads. General-web fallback is decision-only, and native acquisition lacks non-fixture application driving. Credentialed raw and derived response custody remains unfinished local engineering. These gaps precede final verification; unavailable credentials/network do not close them.

The [installed PMC checkpoint](PMC_WIRE_CUSTODY.md) records 342 captured PMC/Dataset/package methods plus 81 disjoint offline provider methods passing on each of Python 3.14.6 and 3.11.15. Its separate 312-check copied-source compatibility matrix overlaps these results and is not additive. This scoped evidence does not complete the pending final-suite table.

The OpenAI Responses provider and OpenAlex, Semantic Scholar, Crossref, arXiv, PubMed, and PMC adapters are implemented behind the sole audited gateway. Their fixture paths test request policy, raw capture before parsing, schemas, provenance, passage locators, retry/failure receipts, secret ownership, and non-evidence semantics. Resolved credentials are never passed to an unverified transport, including the self-reported offline fixture transport, which receives `None`. For a credentialless request, the live gateway can additionally issue a narrow run-bound `audited_transport_execution_authority` only after the exact built-in HTTPS transport consumes a one-shot prepared request; the authority HMAC-binds its request/response/policy/implementation/registry claim and is ledger anchored. A configured credential plus real network use instead stops before request registration, budget consumption, dispatch, response capture, or issuance. Its `0600` key and verifier share the local trust domain, so it is not an external witness and same-process/key/source compromise remains `BLOCKED_LOCAL`.

No live provider or scholarly request is claimed. This checkpoint did not inspect credential presence or retest network availability; source terms, licensing, rate limits, live parsing, model identity/usage/cost and full-text availability remain externally unverified. Live OpenAI is blocked both when its required credential is absent and when it is present on a real-network transport; the latter requires a separately reviewed sensitive-response custody design (`BLOCKED_LOCAL`). PMC production activation remains closed independently of credential availability. Locally signed fixture evidence does not establish a genuine native crossing, deployment signing, response truth or scientific authority.

## Scientific source-authority status

| Boundary | Implemented local authority | Current scientific outcome |
|---|---|---|
| Dataset/Split | Prospective acquisition plan; exact body/license/use; signed credentialless transport; audited semantic use review; atomic raw/manifest/use/authority publication; exhaustive disjoint four-role partition before result visibility | No live dataset crossing; `BLOCKED_EXTERNAL` |
| Result/StatisticalTest | Exact canonical graph preflight, append-only resumable prefix, source-owned promotion replay, typed current bundle completion, optional exact StatisticalTest continuation | Current Local Mac backend lacks scientific isolation; eligible positive Result unavailable (`BLOCKED_LOCAL`) |
| Citation and ClaimSemantics | Inert three-parent `E`, completed `G`, canonical proposal `P`, claim-bound audited `Jref`, final source-replayed `K`; one coarse proposal branch | No live scholarly/provider review or writer-eligible real claim; `BLOCKED_EXTERNAL` |
| Scope/limitation | `Jqual` binds claim text/role/confirmatory/evidence-use/assertion, current Result/Run/latest completion, audited semantic judgment and custody | Cannot mint from a non-evidentiary Result |
| Ablation | Required frozen intervention, deterministic component ID, checked raw output/aggregate/promotion, current Result/Run/Method/Experiment and events, full closure | Inherits Result eligibility; current synthetic/local Result remains false |
| Reproduction | Exact local replay and package read-back | Categorically `scientific_evidence_eligible=false`; never scientific `ROBUSTNESS` |

## Scientific and release status

The final vNext fixture must retain all of the following independent outcomes:

- top-level system-fixture integrity may be `PASS`;
- experiment and clean-rerun evidence remains non-evidentiary; current source has no supported OS-isolation authority, so final verification cannot reinterpret it as scientific;
- claim evidence-use is `SYSTEM_FIXTURE`, so the default scientific writer view is empty;
- scientific soundness is expected to remain `MORE_EXPERIMENTS_REQUIRED`;
- paper verification remains `BLOCKED` and venue classification `NOT_READY`;
- protected confirmation is unused;
- `human_e4_synthesized=false`.

These results are not contradictory. They are the required separation between operational integrity, scientific validity, writing eligibility, and human release authority.

## Failure injection and threat review

The Standard source scan is sealed PARTIAL with 43 review products and three
LOW findings. Installed corrections and scoped tests do not clear excluded
coverage or establish full security acceptance. Exact stopped scopes remain
stopped, not PASS; no new or replacement scan is authorized by this report.
Remaining permissible evidence and D-079 owner triage are separate obligations.

All 20 mandatory adversarial classes are mapped in `vnext_failure_injection_matrix.md`. Exact final captured results are `PENDING`. The final threat review must preserve the remaining boundaries: coherent rewriting of ordinary local hash witnesses; compromise of the local HMAC key, signer/verifier closure, or trusted source; unavailable live providers/literature/GPU; no independent custody/review/E4; and absent Local Mac OS isolation. The HMAC path blocks ordinary caller fabrication but is not an independent signature or transparency witness.

## Capability verdict

The provisional capability ceiling is **`AUTONOMOUS_EXPLORATION_READY`**, not an achieved whole-system or fresh-user readiness verdict. The system can autonomously exercise a bounded synthetic research representation while keeping evidence, soundness, paper, venue, and release gates fail closed. The actual question-to-results workflow, standalone admission, live Colab/provider/literature and scientific requirements remain unfinished. It is not `REAL_EXPERIMENT_PIPELINE_READY`, `RESEARCH_GRADE`, or `SUBMISSION_PIPELINE_READY`.

Architectural comparison with `UPSTREAM_SCIENTISTONE` is reported separately from empirical performance. No comparable local ADRS, MLE-Bench, Parameter Golf, or real research study has run, so empirical superiority is **`NOT_YET_ESTABLISHED`**.

## Real blockers

- Live OpenAI validation needs separately reviewed sensitive-response custody before any credential-bearing dispatch, plus approved network access, service/model access, and cost reconciliation. Credentialless scholarly validation separately needs approved network access, source-specific terms/licensing review, and rate reconciliation.
- Real `GPU_CLOUD` validation needs a CUDA runtime or scheduler, credentials, cost authority, and trusted artifact return.
- Real scientific validation needs appropriate datasets, independent review/custody where required, protected confirmation, external validity, and domain-appropriate evidence.
- Human-only E4 remains unavailable and cannot be synthesized.
- Any remaining internal `NOT_IMPLEMENTED` row in `VNEXT_REQUIREMENTS_MATRIX.md` must be closed or explicitly demonstrated to require an unavailable external condition before completion.

## Finalization rule

Every source, test, configuration, report binding, narrative document, and `.run` checkpoint must be finalized before the project audit. The last repository mutation is:

```sh
/opt/homebrew/bin/python3 -I -S -B scripts/scientist_one_cli.py audit-project
```

Its machine authority is `reports/final_audit.json`. After that command, use only read-only `-B` parsing and hashing and report the audit digest outside the repository; do not edit this document to insert the audit's own digest.

## Five highest-leverage next improvements

1. Run a small, genuinely external, passage-supported research problem through the credentialless controlled literature plane under approved source terms; use the live provider plane only after separately reviewed sensitive-response custody and approved credentials/network access exist.
2. Establish and independently validate an OS-isolated `LOCAL_MAC` scientific worker, then run a non-synthetic clean reproduction with protected confirmation.
3. Validate one concrete scheduled CUDA backend end to end, including cost, preemption, checkpoint recovery, and hash-verified artifact return.
4. Execute one real workload in each required domain, beginning with Generic ML and then Medical Imaging, Operations Research, and Systems under their adapter-specific validity rules.
5. Obtain independent scientific challenge/custody and human E4 review, then run a matched upstream-style benchmark before making any empirical comparison claim.
