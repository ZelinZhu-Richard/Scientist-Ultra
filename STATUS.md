# Scientist-One vNext Status

**DEVELOPMENT / REVIEW, not final acceptance.** `AUTONOMOUS_EXPLORATION_READY`
remains a provisional ceiling from bounded synthetic evidence, not current
whole-system readiness. The [September23 checkpoint handoff](OVERNIGHT_REPORT.md#development--review-checkpoint-september-23-2026)
is the current status: installed V8 is unchanged; old captures are no longer
running; private corrections remain uninstalled; known errors/timeouts and
external/safety blocks remain. The owner now authorizes a public-safe review
branch before final validation/historical import, without waiving final gates.

## Earlier dated checkpoints

The following running-state and Git statements are historical and superseded
by the current handoff. Their test scopes must not be added together.

Current September21 V8 checkpoint: the reviewed15-file correction union is
installed with293 pinned functional inputs. Six whole modules/207 distinct tests
have root-verified enclosing results on both Python3.14.6/3.11.15. The separate
combined468 selection also passes both, with scoped nested-evidence verification.
The separate168-module captures are terminal with2,601 successes/1 error each,
not2,602PASS. The terminal-stop error is a diagnosed test-token provenance mismatch.
A private test-only correction passes its three original methods plus eight new
helper controls on each runtime, with retained evidence verified; installation
and the separate CLI68 fixture correction remain pending. Isolation38
retains37PASS/1ERROR per runtime; the owned-temporary-namespace operations18 retry
also timed out at7200 seconds on both runtimes, with no operations outcomes.
The private gates67 diagnostic also timed out with54 retained progress markers
but no test outcomes. Reviewed private isolation38 and operations18 diagnostics
are now running on Python3.14 under unchanged limits. See
[exact verification scopes](docs/VNEXT_FINAL_VERIFICATION.md). No source inputs
are being changed during these captures, and no timeout is treated as PASS.
The qualified `v0.1.0-recovered-baseline` tag is owner-authorized, but no import,
tag or push has occurred. The older execution checkpoints below are historical;
their pending/running wording does not reopen already completed work.

Pre-integration final-verification selection: 2,022 distinct passing test IDs on each of
Python 3.14.6 and 3.11.15, across retained, hash-reconciled captured runs—not one
full-suite invocation. Failed fixture/portability modules remain excluded and
their original outcomes retained. Long validations and test-only corrections
remain in progress. The historical 364-test report, reconstructed 146-test suite,
and two new baseline checkpoint controls are separate evidence sets. Full
scientific reserve integration, non-fixture literature application driving,
confidential deployment, scientific isolation, durable stall/crash handling,
and release gates remain open;
this milestone is not merely waiting for documentation or a push.

September 21 local continuation: the exact reviewed21-file integration is now
installed, including admission/PILOT failure handling and prepared test fixtures.
Its private120-method evidence passed both runtimes, with independently verified
127 raw nested captures per runtime. The earlier119-case revision retains116
passes and3 failures per runtime; those failed attempts are not reclassified.
Charge34 plus68 dependent controls and eight process-observer controls retain
their separate private evidence. Installed affected-regression runs are now in
progress; the2,022 total above is pre-integration evidence, not a result against
the changed source. Two new lint style findings await the next edit boundary.
Additional observation
fixtures need complete captured-source preparation; provenance guards are not
weakened to make partial copied roots pass. Exact safety-stopped probes remain
excluded, while ordinary owned fixture concurrency is not automatically stopped.
Git is initialized on `main`, tracking the canonical `origin/main` at the owner's
README-only commit. No agent commit, tag or push has occurred. MIT is the resolved
license choice; public-readiness and truthful baseline-history gates remain open.

Current continuation supersedes the earlier running-state descriptions: installed
offline326 passes both runtimes with retained enclosing evidence verified. The
affected393 batch finished with392PASS/1FAIL on3.14.6 and391PASS/2FAIL on3.11.15;
it is not accepted. A generic operational PILOT-pair compatibility failure occurs
on both; a real-clock routing fixture deadline failed on3.11. Original traces remain.
Private observation165, three-output durability131 and corrected GPU routing5
now pass both runtimes with enclosing/nested evidence checked; these are separate,
overlapping scopes, not installed acceptance or an additive total. A bounded
completed-PILOT continuation/compatibility patch is frozen and runs privately
after full independent review. Its compute dependency currently has six failed
assessment cases on each runtime; their cause is being diagnosed, so the new
batch is not accepted. The installed seven-module274 selection is also running.
The private40 contract/amendment fixtures plus165 dependencies now pass on both
runtimes, with all248 retained nested captures per runtime checked. This is private,
overlapping evidence, not an additional installed total. A diagnostic-only compute
fixture patch is being prepared to identify the exact failing source-closure predicate;
the original failures and all real-clock and scientific guards remain unchanged.
Two second-reserve fixtures conflict with current captured-input guarantees;
their original coverage is unresolved, not an earlier-refusal PASS. The owner
has authorized the qualified
baseline tag `v0.1.0-recovered-baseline` in place of the trusted-kernel label;
historical limitations and every public-readiness gate remain unchanged.

September 20 current checkpoint: the updated governing specification is reconciled
without restarting completed work. The [bounded FARS v2 assessment](docs/FARS_DESIGN_REVIEW.md)
is complete and its sole demonstrated adaptation is integrated: operational
completion now enforces the already-frozen minimum output types. Corrected 14
captured tests pass on both Python runtimes, alongside 39 unchanged compatibility
methods; this is not one green 53-test invocation or a full regression. Independent
complete-patch review is closed; initial test errors remain recorded. Existing
contract/plan/controller and evidence-first manuscript owners are reused. Final
verification, preservation and Git/public-readiness work remain; all protected-data,
custody, deployment, live-access and safety/platform blockers remain unresolved.
Earlier dated checkpoints below are historical, including prior FARS sequencing.

Previous bounded result: the coherent PMC wire/custody slice is installed after
complete independent source and mechanical-test review. On Python3.14.6 and
3.11.15, the installed tree passes342 selected captured checks (307 PMC plus35
Dataset/package controls) and81 separate offline provider checks. An overlapping
312-control compatibility matrix also passes against copied installed sources.
The exact26-file installation preserves256 neighboring functional inputs.
These are targeted results, not full-suite, native/live or release acceptance.
Earlier root expectation failures are preserved. The prospective Dataset
contract guard passed119 affected checks and18 portable controls on both runtimes;
both complete-patch and test-migration reviews are root-read. Its exact three-file
change is installed and passes119 affected checks on both runtimes with259 equal
unchanged inputs. These are ordinary targeted tests, not the captured full suite. Synthetic observations
and local socketpairs are not native TLS/live PMC evidence; fixture semantic
sources are not scientific Dataset admission. These overlapping matrices are not
additive full-regression totals. The captured-package compatibility repair is
now installed: 21 private controls and 35 installed captured checks pass on both
runtimes. Independent complete-patch and bounded installation-evidence reviews
are scoped-closed. Its initial parent-binding defect and
startup refusals remain preserved. This is not full-suite evidence. See
[captured test-package execution](docs/CAPTURED_TEST_PACKAGE.md).

**September 19 resumed checkpoint:** Authorized [baseline reconstruction](docs/BASELINE_TEST_RECONSTRUCTION.md) passes 146/146 newly authored tests on the exact recovered kernel on Python 3.14.6 and 3.11.14, plus a separate synthetic seven-command lifecycle and two corrupt-output refusal scenarios. Independent scientific/provenance review closes the bounded reconstruction scope, including three nonpositive lifecycle cases. Historical 364-test evidence and missing original bytes remain separate. The prospective OpenAI schema profile passes 81/81 offline checks on both runtimes, with independent source-correctness closure; three provider-neutral/wire-parity checks also pass. The corrected private PMC persistence/timing/transition matrix passes 115/115 controls on Python 3.14.6 and 3.11.15, with independent source-correctness closure. Original defects and failed tests are preserved. It remains uninstalled, with no native coordination authority; complete response-framing and native cleanup integration are next. None of these results is the unavailable platform risk review, live API validation, confidential deployment, full regression or public readiness. FARS remains queued after complete Phase A; no agent Git mutation occurred.

Earlier September 13 scoped checkpoint: [captured-page publication recovery](docs/LITERATURE_PAGINATION_DESIGN.md)
is installed after independent review. The combined 414/414 affected checks pass
in 191.842s on Python 3.14.6; 16/16 focused checks pass in 8.991s on Python
3.11.15, with no bad outcomes and all 255 installed Python files unchanged.
Recovery can publish a proven capture without another request; it does not prove
managed dispatch. The [next workflow design](docs/LITERATURE_WORKFLOW_DESIGN.md)
has a private implementation candidate, but that candidate is not installed or
acceptance-verified. Its revised test infrastructure passes 3 positive scoped
controls on Python 3.14.6; 35 additional controls remain `UNRUN`. A separate
six-command offline CLI sequence passes with the default resource configuration
unchanged, processing two synthetic pages without scientific or live authority.
The immutable-patch security review was externally stopped; its related work
remains stopped and no completed assessment or integration clearance is claimed.
Earlier counts below describe prior component snapshots, not an additive total.
Full Phase A, other safety stops, external choices, FARS and Git/public-release
obligations remain unchanged. No test process or subagent writer remained active at that earlier checkpoint.

**Current pre-FARS work (September 13):** The installed second-charge/amendment/reserve integration passed622/622 affected checks in3612.042s. The subsequent unsigned PMC capture, bounded decoder and historical-reader integration passed235/235 affected checks in129.890s on its recorded unchanged249-file Python inventory. Both runs had zero failures, errors or skips; their overlapping counts are not added. At that checkpoint, compile-only checks also passed for all249 installed Python files on CPython3.11.15 and3.14.6, with exact equality to the235-test source inventory; this is not runtime compatibility or full regression acceptance.

Private T2/J2 late-capacity controls pass8/8, but broader integration is incomplete. An automated cybersecurity control stopped the related native thread-lifetime investigation; that work and the earlier execution repair are preserved, uninstalled and not retried through another route. Four later native lifetime test additions are unrun, with the tested preimage retained. The separate [descriptive literature-context foundation](docs/PMC_CONTEXT_FOUNDATION.md) passes30 installed tests on both3.11/3.14; it adds no coordinator, live dispatch or authority. The [captured OpenAlex search-response replay](docs/CAPTURED_SCHOLARLY_SEARCH_REPLAY.md) prerequisite is now installed, including a reviewed redirect-outcome correction:287 affected installed tests pass on3.14.6, and22 new tests pass on3.11.14, with all252 Python files unchanged. Earlier overlapping totals are not a full-suite sum. Confidential provider custody awaits a concrete isolated deployment choice. The later [registered SEED/OpenAlex query reader](docs/REGISTERED_SCHOLARLY_SEARCH_REPLAY.md) is installed: 360 affected tests pass on Python 3.14.6 and 22 focused tests on Python 3.11.14, with all 253 installed Python files unchanged. The separate [bounded v2 pagination component](docs/LITERATURE_PAGINATION_DESIGN.md) is now installed: 398/398 affected tests pass on Python 3.14.6 and 36/36 focused tests on Python 3.11.15, with all 254 installed Python files unchanged and no failures or skips. General-web fallback still lacks a non-fixture controller, prospective complete scope, source-owned insufficiency and transport/application execution. The user's MIT license choice is recorded in LICENSE; public-readiness and Git gates remain uncleared. Live full text and the original scientific/release roadmap remain unfinished. FARS waits for the entire pre-existing workstream's stable boundary. No scientific or public-release acceptance follows.

**Current Git state:** The user created and published README-only commit `bba9ec5` on `main`; anonymous remote inspection confirms the exact canonical repository, and `main` tracks `origin/main`. Implementation, tests and private run data are not committed. Preserve the user's documentation bootstrap and build truthful history after it; do not relabel it as the trusted baseline. Full public-readiness, baseline validation, coherent commits and remaining synchronization are pending. No agent commit or push occurred.

The older scoped-checkpoint entries below are historical. Their process, Git and backup-availability descriptions are superseded by the current summary and persistent run ledger; overlapping test counts must not be added.

**Current reserve-lifecycle prerequisite:** A shared current-contract check now distinguishes historical readability from permission to begin new use. It passes154/154 affected tests with unchanged source/test inventory, no skips and a clean independent scoped review. This is a prerequisite only: actual post-release amendment exposure, fresh-reserve allocation, cumulative charging and revised custody execution remain unfinished. No test or implementation subagent remains running at this checkpoint.

**Latest frozen integration:** Operational BEST_OF_N reporting and its scientific-exposure/no-progress integration pass636/636 tests across28 full affected modules in905.570s, with no failures or skips and the complete Python source/test inventory unchanged. Independent scoped review is closed and all13 changed Python files pass Ruff. This establishes the bounded local API, not scientific validity, a product CLI command, full-goal verification or public-release readiness.

**Current integration work:** The reporting checkpoint is complete; earlier focused counts and repaired failures remain in the run ledger and are not added to636. The next local work is the revised-study fresh-reserve lifecycle. Read-only review also mapped confidential provider output, metadata, errors and derivative sinks. Full confirmation lifecycle, sensitive provider custody, scientific isolation, literature integration and the required Git/public-release work remain unfinished. This writer-free checkpoint and its documentation now have a verified complete private backup; nothing has been committed or pushed.

**Latest local checkpoint:** Git-aware audit and captured report-publication integration pass all135 tests across the five full affected/launcher modules, with the complete Python inventory unchanged throughout. Independent scoped review is closed after repairing a genuine late-reinventory race. Operational terminal capture also has a completed independent scoped review: root34 focused tests pass, and the author's affected matrix has170 passes plus one existing opt-in skip. These checks do not complete BEST_OF_N reporting or confer scientific/public-release clearance. Four personal-root documentation references are transparently redacted without changing historical evidence. All project Git operations remain deferred. See [Git-audit scope and limitations](docs/GIT_AUDIT_BOUNDARY.md) and [operational terminal capture](docs/LOCAL_TERMINAL_CAPTURE.md).

**Current D071 reconciliation:** Persisted contract-amendment publication, mandatory contract/design history checks, complete native progress classification, and paired readback freshness are locally integrated and independently scoped-reviewed. Root272/272 tests passed across16 full affected modules with unchanged source/test inventory and clean Ruff, including real operational multigeneration, late-exposure, orphan, direct-consumer and concurrency controls. Full fresh confirmatory-reserve/protocol integration remains open. Other remaining local work includes separate BEST_OF_N reporting, credentialed-live sensitive raw/derived custody, complete local scientific isolation, and selected literature acquisition/workflow integration. Existing limited Seatbelt host integration passed1/1 outside the nested sandbox; broad-read isolation/resource containment/scientific attestation are still absent. D070 remains scoped-verified. Exact hashes, scope and retained earlier failures are in `.run/STATE.json`; this is not scientific success or whole-goal acceptance. User-required public-safe Git history and synchronization are also pending, with preservation before reconstruction and no push before public-readiness approval.

**Git preservation checkpoint:** A complete earlier integration checkpoint is preserved in a verified owner-only archive outside the repository; later implementation is retained in the working tree. Exact baseline source/configuration recovery is complete for26source+3config files, but the original11-test suite was not recovered in the bounded search. Baseline commits/tag wait for another backup or the user's decision on an explicitly reconstructed verification suite, followed by validation. Project Git remains uninitialized: no staging, commit, tag, fetch or push. Native Git tool checks and isolated synthetic repositories are permitted only for audit-adapter tests; they are not project history. No public-readiness audit has passed. See [reconstruction evidence and limitations](docs/GIT_HISTORY_RECONSTRUCTION.md). The original implementation remains active.

**Protocol lineage scoped verification:** Persisted protocol-revision publication and full replay now pass179/179 affected tests, with unchanged source/test inventory and a clean independent postpatch review. The component binds exact amendment, predecessor and parent/child contract identities; preserves historical source populations; and checks chronology, concurrent source changes, orphan recovery and contradictory ownership records. An existing pure-parser bug and review-found publication/selection defects were reproduced and repaired. It remains explicitly non-authorizing: fresh-reserve, Dataset/Split, D/STARTED, custody and cumulative charging integration are unfinished. Earlier87/151/171/177/272 checkpoints overlap and are not additional combined totals. This is scoped local operational evidence, not a completed scientific pipeline.

**Public-release preparation:** Conservative ignore defaults and a [public-release policy](docs/PUBLIC_RELEASE_POLICY.md) are prepared. Effective Git ignore verification and all content, confidentiality, rights and claim audits remain pending. No local files were deleted, no scientific audit rules changed, and nothing has been committed or pushed.

**Reporting scope:** Source-owned admission, exact native attempt archival, predeclared selection and report recovery/readback are locally integrated. Adverse numeric observations and missing seed coverage remain explicit; all-adverse cohorts have no selected result. Operational admission counts as possible exposure in current scientific freshness/progress checks; NON_EVIDENTIARY does not mean unobserved. Disabled isolation requires conservative same-population exclusion, with no cross-run or unregistered-file non-exposure guarantee. Selected performance is never representative average or scientific evidence. See [operational reporting](docs/OPERATIONAL_BEST_OF_N.md).

**Current build state:** The supported synthetic Research OS path is integrated, but non-blocked scientific implementation and integration are still in progress. The fixture materializes canonical state through the existing `ArtifactRegistry` and `EventLedger`; executes multi-round literature/citation/ranking and registry-checked novelty; admits a bounded reviewed-template autonomous component; captures adaptive execution-plan custody; executes a real Local Mac child plus clean rerun; derives registry-resolved discovery, claim, superiority-diagnostic, soundness, terminal-outcome, and paper state; and stops at conservative scientific and paper gates. Separate public scientific-authority boundaries cover signed credentialless live gateway execution, prospective `Dataset`/`Split`, Result/`StatisticalTest`, canonical ClaimSemantics, claim-bound citation support, exact qualifiers, and source-owned Ablation. Their presence is not proof of complete scientific pipeline liveness.

**Open final verification:** The protocol-scoped GPU-requirement terminal is integrated and independently scoped-reviewed, with root414/414 affected tests passing. The review found and closed a real publication-order defect; prospective GPU policy now selects its route and preserves absent-policy legacy behavior. Complete acyclic native Venue/manuscript/paper canonical readback and final Venue publication CAS have the earlier independently reviewed788-test checkpoint. Existing estimates and GPU plans do not prove hardware necessity. Every material requirement still needs final reconciliation, followed by frozen full regression, security and end-to-end/recovery verification; component tests do not establish a positive scientific lifecycle. Exact checkpoints are in `.run/STATE.json` and `.run/ISSUES.md`.

The GPU requirement checker reopens actual prospective sources for one mandatory CUDA-device timing obligation. A distinct codec shares existing compute publication/recovery and supports canonical `FULL_VALIDATION_REQUIRES_GPU_CLOUD` only as an operational blocker: no scientific evidence, claim IDs or spending authority. Real operational tests cover both admission orders, both publication call orders, canonical readback, crash recovery and changed-source refusal. The414-test rerun includes the fake-GPU getter repair and an explicitly synthetic memory-probe seam in one old resource-pair test; production resource limits remain unchanged. CUDA compilation/execution, binary equivalence, availability and scientific adequacy remain UNTESTED; the retained evaluator ran only on synthetic data in isolated standard-library Python.

**Current scientific state:** `MORE_EXPERIMENTS_REQUIRED`. The fixture paper is `BLOCKED`, venue status is `NOT_READY`, final release is `BLOCKED_SCIENTIFICALLY`, and E4 is absent. A system-fixture `PASS` is not a scientific pass.

**Final evidence freeze:** **PENDING**. No vNext run ID, artifact/event/object count, digest, full-suite total, audit identity, or final comparison is authoritative until source and documentation stop changing and final verification completes.

**Historical D068 paper-terminal checkpoint:** The full same-round paper replay and exact mechanical terminal Decision are integrated. Independent Astra MAX review closed after repairing the incorrect equation of empty confirmation authority with a run-less source; full source ownership and legacy run-less behavior remain intact. Root753/753 affected tests passed in196.826s with unchanged complete Python source/test inventory and clean Ruff; separate121/121 compatibility checks passed. All three bound-state checks, exact source identities, chronology, freshness and complete stored-verification equality remain mandatory. D069 now extends native Venue readback and has separate current788-test evidence; the D068 counts remain historical. This is mechanical/negative/source-level verification, not a positive scientific lifecycle or capability upgrade.

**2026-09-06 ablation checkpoint (supersedes earlier component-level descriptions):** The native fixed-policy numerical intervention path now has prospective/executed source replay, outcome-neutral primary Result integration, descriptive authority/canonical Ablations, complete cohort replay, existing-wire Soundness publication/readback, and exact same-snapshot plural-audit Soundness/R5/E2 composition. Native Ablations always retain `scientific_evidence_eligible=false`; complete operational coverage still leaves scientific adequacy `UNTESTED`. Independent scoped review qualified no remaining consequential finding after repairs for source routing, older audit compatibility, and caller-selected legacy-receipt bypass. The current affected matrix passed 716/716 tests, the separate evaluator/bundle/terminal/paper compatibility run passed 111/111, and the independent focused run passed 29/29. These are not a final full-suite capture or a genuine positive scientific lifecycle. Scientific external-validity/GPU-necessity integration, the remaining paper/venue lifecycle and final whole-goal verification remain unfinished.

## Implemented vNext boundary

**2026-09-06 external-validity checkpoint:** All currently admitted scientific Generic-ML profiles retain GENERALIZATION `UNTESTED`, including older training/projection profiles. A separate deterministic procedure inspects the complete source-owned scientific cohort and its within-Dataset scope using existing execution/review receipts; completion still means external adequacy `UNTESTED`. Root651/651 affected tests and independent34/34 focused tests passed. Independent review closed after preserving the truthful incomplete-Soundness path. No external validation or successful scientific issuance was performed.

- The guarded CLI exposes `research-os-fixture` only through the captured `-I -S -B` launcher and admitted orchestrator root.
- Each fixture invocation validates required inputs, atomically reserves a new run identity, and atomically publishes `fixture-operation.json` as `IN_PROGRESS`, `FAILED`, or immutable `COMPLETE`. Reusing a run ID is rejected.
- Canonical typed research objects are registered and ledgered before downstream consumption. Registry closure, ledger chaining, provenance parents, and state validity are checked again before completion.
- The controlled literature fixture executes the required seed-search, citation-expansion, relevance-filtering, full-text-review, and disconfirming-search rounds. Its bounded expansion plan/execution, citation graph, target-bound evidence ranking, normalized records/full text/passages, and investigation state are registered and re-resolved before novelty clearance. Live scholarly access remains `BLOCKED_EXTERNAL`; synthetic records do not establish real novelty.
- The live gateway has one narrow run-bound execution-authority path for credentialless requests. Only the exact built-in HTTPS transport, after consuming its one-shot prepared-request capability, can HMAC-authenticate the captured request/response claim and ledger-anchor it. A configured credential plus real network use instead produces `BLOCKED_EXTERNAL` before gateway request registration, request-budget consumption, dispatch, response capture, or authority issuance. Resolved credentials are never passed to an unverified transport even if it self-reports offline; the fixture transport receives `None`. The key and verifier are local: this prevents ordinary caller-authored authority, but provides no external witness and does not resist same-process, same-user key, or trusted-source compromise (`BLOCKED_LOCAL`). No live request was made.
- Scientific Dataset use is prospective and source-owned: an exact acquisition plan precedes access; source bytes, license, intended processing/derivative/redistribution scope, signed credentialless transport, frozen Evaluation Contract, and audited semantic use judgment are replayed; publication is all-or-nothing or resumable only from an exact inert prefix; and the complete four-role `Split` partition is frozen atomically before Run/Result visibility. Live source validation remains `BLOCKED_EXTERNAL`.
- The provider fixture exercises the provider-neutral/OpenAI Responses structured-output path through an unverified fixture transport that receives no resolved credential. It is advisory and non-scientific. A missing required OpenAI credential blocks availability; a present credential with real network use is also `BLOCKED_EXTERNAL` until separately reviewed sensitive-response custody exists (`BLOCKED_LOCAL`).
- Bounded autonomous implementation admits only reviewed closed templates and bounded parameters. Provider output cannot supply code, argv, paths, dependencies, shell, or network policy; exact worker bytes, proposal/admission receipts, frozen spec, outputs, and deterministic semantic recomputation are registry-bound and non-evidentiary.
- `LOCAL_MAC` runs `/usr/bin/python3 -I -S -B scripts/vnext_fixture_experiment.py` twice against frozen inputs and compares declared output manifests. Backend adaptive-plan bytes and their frozen-spec/submission/collection binding are captured in the registry. Technical validation and clean system reproduction can pass, but scientific evidence remains `NON_EVIDENTIARY` because OS-enforced isolation is unavailable and network status is `UNKNOWN_UNATTESTED`.
- `GPU_CLOUD` has both the deterministic fixture lifecycle and a scheduled-provider contract for plan-bound idempotent submission, queueing, retries/preemption, checkpoints, and bounded artifact return. These boundaries remain externally `UNTESTED`, use no live credentials or hardware, and produce no scientific evidence.
- The four required domain status families—Generic ML, Medical Imaging, Operations Research, and Systems—have typed validity logic. Time Series and Recommender Systems adapters are additional. The integrated fixture exercises Generic ML only; final frozen domain test evidence is pending.
- Discovery promotion is recomputed by a pinned reviewed evaluator from exact registered specs, manifests, plan bindings, seed outputs, ablations, controls, robustness, falsification evidence, and ledger provenance. Diagnostic-only values cannot authorize promotion. The integrated state retains promoted, negative, and null branches; final frozen discovery hardening evidence remains `PENDING` until handoff.
- Claim verification resolves all mandatory typed support classes through registered artifacts and independent receipts. Eligibility is limited to the stated synthetic fixture scope and does not make the Local Mac comparison scientific evidence.
- Result-v2 materialization performs a zero-write graph preflight and append-only commit for the exact `Method`/`Implementation`/`Hypothesis`/`Dataset`/four `Split` roles/metrics/baselines/`Experiment`/`Run`/`Result` graph. A later exact `StatisticalTest` extension replaces the Result-only completion authority; stale completions and partial/colliding continuations fail closed. Scientific eligibility remains inherited from source-owned promotion, which no current unsandboxed Local Mac run satisfies.
- Canonical ClaimSemantics enforces one coarse proposal branch for the exact run/graph/claim/dependencies and independently reviews every writer-visible field. Scientific citation authority is acyclic: inert three-parent citation evidence `E`, completed graph `G`, proposal `P`, claim-bound reference judgment `Jref`, then final ClaimSemantics `K`. Scope and limitation nodes require exact `Jqual` authority over the current Result/Run/bundle completion and claim qualifiers. Favorable parallel judgments, semantic rewrites, and stale bundle completions become ambiguity or collision, not selectable authority.
- Scientific v2 Ablation authority replays the required frozen-contract intervention, deterministic component ID, raw ablation output, aggregate/promotion sources, current Result/Run/Method/Experiment state and events, and complete custody closure. Legacy ablation artifacts cannot enter that lane, and eligibility can only inherit the exact Result.
- Synthetic reproduction is categorically non-scientific. A matching legacy or vNext system replay remains `scientific_evidence_eligible=false` and cannot satisfy a scientific `ROBUSTNESS` edge.
- Value-only superiority validation is explicitly `DIAGNOSTIC_ONLY`. The checked promotion API requires independent registry-resolved scientific execution eligibility and recomputes the comparison; the non-evidentiary fixture cannot satisfy it and its diagnostic is excluded from paper authority.
- Soundness binds exactly 15 dimension receipts, 14 Challenger category reviews, and every finding. A major unresolved external-validity challenge forces `MORE_EXPERIMENTS_REQUIRED`.
- The exact 11-outcome terminal registry derives and freezes honest endpoints as registry artifacts plus canonical decisions without mutating the legacy macro-state machine. The integrated fixture materializes `MORE_EXPERIMENTS_REQUIRED`.
- Paper construction and verification re-resolve a registry-derived authoritative bundle. The fixture retains false scientific baseline/statistics controls and hard blockers for non-scientific claims, unsupported novelty, scientifically ineligible reproduction, and the unresolved challenge; venue classification remains `NOT_READY`.
- Human gates and scientific gates are independent. The system never constructs or implies E4.

## Historical pre-vNext baseline

The preserved August29 record reports the following pre-vNext results. They are
historical claims, not current independent reproduction of the missing original
test suite or architecture packet:

- fresh captured suite: 364/364 passed; report SHA-256 `73dee18929495015a1693ead7cf6849ae85cb96430b9887cfa7939c78ed28f36`;
- architecture controls: 15/15 retained and targeted validator 4/4; report SHA-256 `3d6c80c7f63514c9b711c9d2b98b7512b02550870cda567f18b3a82de112580e`;
- fresh baseline demo: `run-20260829T170951Z-28d371b6e3`, `READY_FOR_HUMAN_REVIEW` / `COMPLETE_DEMO_ONLY`, 21 events, 58 artifacts;
- historical handoff demo: `run-20260812T204930Z-299b1dad55`, reverified without rerunning confirmation;
- pre-documentation audit: `PASS`, report SHA-256 `d63e984d31541bdce65165193cd3015f282274ca3a91ee099bc09416b60773fa`.

The original [baseline record](docs/baseline_before_vnext.md) is unchanged.
[Recovery and reconstruction](docs/BASELINE_TEST_RECONSTRUCTION.md) separately
verify the recovered source and146 reconstructed controls, with incomplete
historical coverage and11 scopedPASS/3PARTIAL/1BLOCKED architecture dispositions.
Neither evidence set establishes final vNext acceptance.

## Pending final authorities

| Authority | Current value |
|---|---|
| Final vNext run ID | **PENDING** |
| ArtifactRegistry count and closure digest | **PENDING** |
| EventLedger count, head, and file digest | **PENDING** |
| Canonical-state object count and snapshot digest | **PENDING** |
| Integrated summary SHA-256 | **PENDING** |
| Final captured suite count/report SHA-256 | **PENDING** |
| Final architecture-control report/validator result | **PENDING** |
| Final project audit snapshot/report SHA-256 | **PENDING**; after the last mutation, read from `reports/final_audit.json` and report externally |
| Final legacy regression/reproduction/package evidence | **PENDING** |
| Final upstream empirical comparison | **NOT ESTABLISHED** |

## Hard limitations

- No OS-enforced Local Mac experiment sandbox has been implemented; process isolation by `-I -S -B` is not a substitute.
- No live literature, provider, or GPU execution has been validated.
- Credential-bearing live response custody is not implemented (`BLOCKED_LOCAL`), and actual provider/network validation remains `BLOCKED_EXTERNAL`. Credentialless scholarly live routes remain structurally reachable.
- No proprietary or real research dataset, external novelty review, genuine independent custody, or independent scientific reviewer is available.
- Most local artifacts remain content-hash and ledger protected, not externally signed. The gateway's HMAC authority is the narrow exception; because its key and verifier share the local trust domain, it is still not an independent witness against coherent same-user/key/source replacement.
- The vNext operation receipt is bounded operational status, not a second scientific provenance authority and not a recovery license for a partial run.
- E4 is absent; scientific publication, submission and research-data release remain blocked. Owner-authorized software-source synchronization with the canonical GitHub repository is separate and still requires every public-readiness and Git-release gate.

The final audit will be created only after every other repository mutation. Its machine authority is `reports/final_audit.json`; no documentation edit will be made afterward merely to copy its digest.
