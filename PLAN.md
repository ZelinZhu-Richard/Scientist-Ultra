# Scientist-One vNext Execution Plan

## Current bounded milestone — owner D083, September 24

The D083 amendment makes ML-first development the current priority: Generic ML
with a first-class CV profile, then ML+OR, ML+Management Science & Engineering,
and offline-only quantitative-finance ML. The first CVPR topic remains
`AWAITING_OWNER_TOPIC`. The amendment authorizes bounded offline request/worker
preparation, not live experiments or scientific authority.

Latest verification closes the original192-module / 3,294-distinct-ID
Python3.11.15 selection: parent `58644`, exit0, with reviewed closure
`68f0a148cd8f623f9f12d53477cbe04dae64db525536ee1f111b38446acaa749`, result
`395aeae17a4777313b2dfa01a2a1f95fe7711821abb4101512e7860464dc5783`, and 885
files. It is an enclosing-source selection only. Source freeze is
`8f5b36693b67312a181c3fa0f87491623b48a2b85f1ec12e90027890767db973`. This does
not pair with historical V9 Python3.14 evidence or establish a full suite,
scientific, nested/helper, E4, or cleanup result. Existing24 focused and18
operations checks remain separate; do not add their historical/private counts.

| Work | Current state and next boundary |
| --- | --- |
| D083 offline ML/CV intake and Colab worker preparation | Root owns bounded source work; offline byte preparation/inspection DONE at component scope after non-author review and15 paired captured tests. Concrete transport and question-to-results workflow remain incomplete. Reuse existing owners; no new authority or live spending. |
| Colab consumer CLI | Official automation CLI exists; `colab` is absent from PATH. Exact installed release/runtime, auth, plan entitlement, and live balance are unknown. No install, account access, allocation, or spending occurred. Pin reviewed source; require explicit auth mode. CU balance/rate visibility is not a hard per-job or cumulative spending cap. |
| COLAB-001 | Awaiting owner triage; no defect conclusion or implementation claim. |
| D082 / E0 / legacy CLI | Admission implementation, E0 changes, and stronger upgraded-CLI legacy restriction remain unapproved. |
| gates67 / Claude | The 7,200-second gates67 capture ended TIMEOUT; cause, cancellation, cleanup, and all67 outcomes remain UNKNOWN. No retry. Claude credential-lock was not retried. |
| Remaining original obligations | Current-source synthetic lifecycles, architecture structural checks, source compilation and qualified recovered-baseline publication are closed at their stated scopes. Finish evidence reconciliation, capability/docs/Git report and final audit; preserve every safety stop and unresolved external/scientific gate. |

The published implementation checkpoint is `0c48e60d2a7ca05a00bb223c39abe7777b99c108`;
`main` remains `bba9ec5`. This development checkpoint does not claim a final
release or Goal completion. Detailed dated V9 and earlier checkpoints below
remain historical evidence; their former “remaining192 NOT_STARTED” statements
are superseded by the current result above.

### Finite remaining sequence — existing obligations, not new scope

Root owns integration and evidence closure. The305-input source is frozen;
both synthetic lifecycle ladders are closed and the qualified baseline is
published. Four unchanged architecture structural checks pass both runtimes.
Remaining work is existing-evidence reconciliation, capability/docs/Git report and development
publication, followed by the canonical audit. Do not repeat completed selections
or lifecycles. Failures, timeouts, stopped work and external blockers remain
explicit; none is waived by the completed Python3.11 selection.

| Deliverable / root owner | Dependency | Observable exit |
| --- | --- | --- |
| Colab offline byte preparation — DONE and published | Existing frozen request/spec/output contracts, no authority edits | Complete non-author patch review and15 paired captured input/hash/schema/negative-result/failure checks; root raw/source joins closed. No live or collection authority claim. |
| Question/configuration path | Existing admitted workspace and original scientific/controller owners; topic-independent inputs only | Exact supported intake path and first actual blocked stage documented; no fixed fixture mislabeled user research. Any consequential expanded repair is queued, not implemented. |
| Source freeze and current synthetic lifecycle — DONE at technical scope | Frozen305 proof8204cca1, no source changes | Guarded vNext3 closureddf4f878 and legacy8 closurea236376b retain raw/source/status bindings. Scientific outcome remains blocked; completed-run skip is not crash recovery. No repeat is needed for unchanged inputs. |
| Architecture packet / four structural checks — DONE at structural scope | Report061277c0/review64020b29, inputs unchanged | Same4 pass both runtimes510c1181/093445be;5 scopedPASS/8PARTIAL/1BLOCKED/1PENDING, historical ADR0001 unchanged. No invented15/15. |
| Qualified recovered-baseline history — DONE | Fresh complete private preservation, exact47 candidate and all reachable-content checks | Import4762578 and qualified tag remotely verified on additive `codex/recovered-baseline`;146 reconstructed methods, not original364. Main/review history unchanged. |
| Accepted development publication and final report/audit | Stable workspace, exact staged/outgoing content checked | Coherent review-branch commits, normal push and remote SHA verification; main unchanged. Final capability/Git report and canonical audit only after last applicable input mutation. |

The question-driven research workflow remains INCOMPLETE. D082 admission
implementation and compatibility choices are unapproved. gates67 TIMEOUT,
isolation verification limits, external access/data/custody, safety stops, and
historical uncertainty remain distinct unresolved entries in the existing
ledger. Do not infer an experiment, E0 authority, hard spend cap, or final
release from the current development checkpoint.

## Preserved V9 verification checkpoint

September 23, 2026 checkpoint, incorporating the Python 3.14 terminal observation at 16:19:20 UTC: V9 is installed and frozen with 300 functional inputs.
The same 125 selected IDs pass on Python 3.14.6 and 3.11.15 at enclosing scope;
a disjoint 3,294-ID selection passes on Python 3.14.6 only. This is 3,419 distinct
selected current IDs on Python 3.14, with only 125 currently paired; remaining192
on Python 3.11 is NOT_STARTED. Nested/helper behavior remains NOT_ASSESSED by
the enclosing closures. These are not whole-suite, security, scientific or
release acceptance.

Maintain the V9 functional freeze; preserve closed results and the still-unstarted
Python 3.11 remainder without inventing a paired 3,294 result. Complete only
already-authorized remaining verification at its recorded scope. Do not restart
FARS, reconstruct old counts, reopen the sealed PARTIAL scan or substitute for
stopped work. Separate operations, lifecycle/recovery/reproduction/package,
architecture, qualified Git history and final audit obligations remain.

The prior verified V8 checkpoint is `f6b3d92` on
`review/vnext-checkpoint`. Publication identities and observed Git status for
the accepted V9 delta are recorded in the checkpoint handoff. Qualified
recovered-baseline import/tag and final feature-history integration remain
pending; `main` stays at the owner's README-only bootstrap. V9 is a development
checkpoint, not a final release.

Under the owner's D-079 rule, report new consequential findings before repair
or an expensive new campaign; do not delegate a repair. A single reviewed,
private, observability-only gates67 diagnostic retains all 67 methods and the
7,200-second bound. It is approved only after active regression is terminal and
the safe V9 push and remote verification have completed, if ready. No automatic
rerun or repair follows it; owner triage is next. The initial bounded Claude
diagnosis is complete; the later credential-lock denial is not being retried.

The [current handoff](OVERNIGHT_REPORT.md#current-handoff--owner-d085-september-24-2026)
controls current evidence and ordering; historical plans below do not authorize
stopped tests, unchanged retries, budget increases or new implementation work.

## Earlier planning checkpoints

Current September21 execution is recorded in [final verification](docs/VNEXT_FINAL_VERIFICATION.md):
the V8 correction union is installed and frozen;207 tests have scoped enclosing
acceptance on both runtimes, other final captures remain pending, and isolation
timeouts are unresolved. Continue those existing captures without restarting
them. The owner-authorized baseline tag is `v0.1.0-recovered-baseline`; no import,
tag or push has occurred. Earlier dated execution instructions below are
historical, not directions to repeat completed work or request FARS permission.

September20 checkpoint: [FARS assessment and A01](docs/FARS_DESIGN_REVIEW.md)
are scoped-closed. Continue the original permitted final verification,
documentation/preservation and Git/public-readiness prerequisites. Do not create
another plan/claim authority or implement optional note indexing, portfolio
infrastructure or generic DAG machinery without a demonstrated milestone gap.
The updated meta-spec routing applies prospectively; actual runtime configuration
is unverified where not exposed. Resolved owner decisions and original blockers
remain. Historical baseline, reconstructed baseline and current regression counts
must stay separate; earlier pre-FARS sequencing text below is historical.

Previous checkpoint: the independently reviewed PMC wire/custody slice and portable
regressions are installed. Captured342 plus disjoint offline-provider81 pass on
both runtimes; overlapping installed-source312 also passes. The exact26-file
installation preserves256 neighbors. Do not restart this implementation or infer
native/live activation from its offline evidence. Earlier private root315 and its
correctness review are closed. The captured-package repair is
installed and passes35 targeted captured checks on both runtimes with independent
installation-evidence closure; do not redesign it. Installed Dataset119 is
complete on both runtimes. Preserve all failed evidence, historical readers and
fully preflighted no-op retries; do not reopen closed components or live barriers.
Scientific fresh-reserve authority remains unresolved. Do not redo closed
components or treat private correctness as installed/native/live authority.

Current September 19 work: the reconstructed 146-test suite passes both runtimes and independent scientific/provenance review is closed at its stated scope. PMC framing, retained-deadline accounting, supplied local-store outcomes and versioned wire replay now have coherent private350 coverage, with earlier defects/failures preserved. The portable342 selection is307 PMC behaviors plus35 Dataset/package regressions, not an additional350 tests or a full-suite result. Genuine native acquisition/lifetime and live authority remain unverified and activation remains closed. Continue remaining pre-FARS dependencies without restarting the already-closed D068–D071 paper, Venue, GPU-terminal, amendment/history, protocol-lineage or reporting components. The original missing-test choice is resolved; historical source uncertainty remains. No stopped investigation is retried or treated as passed. Preserve the pre-FARS integration boundary and all Git/public-release requirements.

Earlier September 13 dependency, still blocked rather than accepted: complete the selected
[managed literature-acquisition workflow](docs/LITERATURE_WORKFLOW_DESIGN.md)
through the existing command, registry, ledger and resource owners. A private
candidate exists but is not installed or acceptance-verified: its test
infrastructure passes 3 positive scoped controls and has 35 additional controls
`UNRUN`. A separate six-command offline CLI sequence passes under unchanged
default resource limits. Required immutable-patch security review is externally
stopped, without retry/rerouting or integration clearance. Its publication-only
recovery prerequisite is installed: 414/414 affected checks on Python 3.14.6 and
16/16 focused checks on Python 3.11.15 pass, with the complete 255-file installed
Python inventory unchanged. Do not treat that offline library evidence as the
application workflow. Phase A remains incomplete; FARS is unread and Git/public-
release gates remain unchanged.

## Objective and stopping condition

Evolve the verified trusted kernel into Research OS vNext without weakening provenance, custody, recovery, reproducibility, scientific validity, or human authority. The stopping condition is implementation, integration, adversarial testing, regression verification, documentation, and an evidence freeze for every non-blocked requirement. Unavailable credentials, services, hardware, datasets, or independent authority must remain explicitly `UNTESTED` or `BLOCKED_EXTERNAL`.

The final capability classification is conservatively capped at `AUTONOMOUS_EXPLORATION_READY` unless stronger evidence is produced. The current build does not justify `REAL_EXPERIMENT_PIPELINE_READY`, `RESEARCH_GRADE`, or `SUBMISSION_PIPELINE_READY`: Local Mac's limited opt-in Seatbelt mode is not complete scientific isolation, and live external paths remain unvalidated.

The user's Git completion gate remains truthful history and public-safe
synchronization with the canonical Scientist-Ultra remote. The published V8
review checkpoint preserves the README bootstrap; it is not the recovered
baseline or V9 acceptance. Complete private preservation preceded V9 installation.
Recovered source/configuration and 146 explicitly reconstructed controls remain
a separate evidence set, with missing original tests and architecture limits.
The qualified recovered-baseline import/tag, exact final ancestry, public claims
and staged/reachable-content review, and verified synchronization remain pending.
Do not merge unfinished vNext into `main` or invent historical feature states.
See [the reconstruction record](docs/GIT_HISTORY_RECONSTRUCTION.md).

## Preserved architecture contract

- `ArtifactRegistry` and `EventLedger` are the only scientific artifact/event authorities.
- `ResearchStateRepository` materializes canonical objects through those authorities; Markdown remains derived.
- Deterministic validation precedes model judgment.
- Exploration and confirmation remain distinct; negative, null, inconclusive, and falsified results are retained.
- Human gates and scientific gates remain independent; E4 cannot be synthesized.
- External content is untrusted evidence and may enter only through audited gateway/adapters.
- `LOCAL_MAC` and `GPU_CLOUD` share scientific contracts even when one backend is unavailable.
- Domain-specific validity stays inside provider-independent domain adapters.

## Completed implementation phases

1. **Baseline preservation and bounded reconstruction.** Preserve the historical reported364/364 and15/15 in `docs/baseline_before_vnext.md`. Exact recovered source plus146 semantic reconstructions and separate synthetic lifecycle controls are verified independently; the complete original test/report evidence remains `HISTORICALLY_UNVERIFIABLE`. Architecture11 scopedPASS/3PARTIAL/1BLOCKED and the qualified tag do not imply full trusted-kernel acceptance. See `docs/BASELINE_TEST_RECONSTRUCTION.md`.
2. **Upstream analysis.** Reverse-engineered upstream ScientistOne from primary artifacts and separated architectural comparison from unestablished empirical performance.
3. **External boundary.** Added audited egress policy, credentialless live capture/authority receipts, provider-neutral model contracts, a strict OpenAI Responses adapter, controlled scholarly acquisition/reference verification, and a pre-request kill switch for credential-bearing real-network egress. Resolved credentials are withheld from every unverified transport, including self-reported offline fixtures, and common reversible credential forms are rejected at ordinary custody sinks as defense in depth.
4. **Scientific design and literature authority.** Added executable multi-round investigation, bounded citation expansion, scholarly evidence ranking, the checked Problem Investigator, registry-resolved novelty clearance, hypotheses, baselines, metrics, splits, resource estimates, and frozen Evaluation Contracts.
5. **Bounded autonomy, discovery, and execution.** Added reviewed closed-template implementation autonomy; registry-evaluator-derived branch promotion with negative/null retention; provider-neutral experiment specifications; adaptive-plan custody; a real Local Mac child execution path and clean system rerun; and fake plus scheduled GPU boundaries that remain non-evidentiary and externally `UNTESTED`.
6. **Domain validity.** Added meaningful typed checks for Generic ML, Medical Imaging, Operations Research, and Systems, plus Time Series and Recommender Systems. The integrated fixture exercises Generic ML only.
7. **Claims, promotion, and gates.** Added registry-resolved evidence graphs, content-bound support and resolution receipts, deterministic statistics, a diagnostic-only value path and checked superiority-promotion authority, exactly 14 Challenger categories, exactly 15 soundness dimensions, and separately configurable human gates.
8. **Research representation, terminal outcomes, and paper path.** Materialized every required canonical object family, implemented the exact 11-outcome registry/decision boundary, and materialized the fixture's derived `MORE_EXPERIMENTS_REQUIRED` outcome; constructed a registry-derived evidence-first paper candidate; re-resolved table/prose/method/claim authority; excluded the fixture superiority diagnostic; and forced hard blockers to `BLOCKED` / `NOT_READY`.
9. **Guarded integration.** Added `research-os-fixture` to the captured CLI, atomic run-ID reservation, atomic `fixture-operation.json` status receipts, immutable completion, and fail-closed failure semantics.
10. **Source-owned scientific authority.** Added a run-bound HMAC-authenticated credentialless live-gateway execution receipt; prospective, license/use-reviewed and atomic `Dataset`/four-role `Split` authority; preflighted append-only Result-v2/optional `StatisticalTest` bundles with current completion authority; canonical outcome-independent ClaimSemantics proposal slots; the acyclic `E -> G -> P -> Jref -> K` citation path; exact Result/Run/completion-bound `Jqual` for scope and limitations; source-owned v2 Ablation authority; and a categorical rule that synthetic reproduction is never scientific robustness evidence.

## Integrated fixture contract

The bounded fixture must continue to demonstrate all of the following simultaneously:

- system-fixture integrity may be `PASS` only after registry, ledger, canonical-state, semantic-output, and gate checks pass;
- controlled synthetic literature and fixture-transport provider paths use no live network and confer no real external validation;
- literature executes all required investigation rounds, citation expansion, graph/ranking construction, and registry-checked fixture novelty without claiming external novelty;
- provider-selected implementation is confined to reviewed closed templates and bounded parameters and remains non-evidentiary;
- the Local Mac subprocess and clean rerun are real, adaptively planned, custody-bound executions whose technical validation is distinct from their `NON_EVIDENTIARY` scientific status without OS sandboxing;
- fake and scheduled GPU backends remain boundary-tested only and `UNTESTED` externally;
- registry-resolved promoted, negative, and null branches remain visible;
- the synthetic claim is scope-limited and cannot inherit scientific authority from a non-evidentiary experiment;
- fixture superiority remains `DIAGNOSTIC_ONLY`; no scientific promotion receipt is minted or admitted to paper authority;
- all 15 soundness dimensions and 14 Challenger categories are bound before verdict derivation;
- one of the exact 11 terminal outcomes is registry-materialized without rewriting the legacy state machine;
- the unresolved external-validity challenge yields `MORE_EXPERIMENTS_REQUIRED`;
- paper verification remains blocked and venue status remains `NOT_READY`;
- E4 remains absent and final release remains `BLOCKED_SCIENTIFICALLY`;
- top-level `PASS` is never presented as scientific, publication, or submission approval.

## Final verification sequence

The following September13 paragraph is retained as a historical checkpoint.
Its FARS-unread and pending-component statements are superseded by the current
checkpoint above; the remaining numbered final-verification obligations still
apply within the recorded safety boundaries.

The current September13 checkpoint supersedes older dependency descriptions below: installed second-charge integration622 and subsequent unsigned PMC235 affected checks pass on their separately recorded source inventories. Private T2/J2 capacity8 passes but full integration remains incomplete. Native lifetime investigation and the earlier execution repair are safety-stopped; do not retry or reroute them. The separate non-authorizing literature-context foundation is installed and passes30/30 tests on both3.11/3.14, with unchanged251-file inventory and original249 files preserved. Complete-captured OpenAlex HTTP response replay is now installed: 287 affected tests on3.14.6 and22 portable tests on3.11.14 pass with unchanged252-file inventory. The later registered SEED/OpenAlex goal/target/plan/result join is installed: 360 affected tests on Python 3.14.6 and 22 focused tests on Python 3.11.14 pass with all 253 installed Python files unchanged. The separate bounded v2 pagination component is now installed and scoped-verified: 398/398 affected tests pass on Python 3.14.6 and 36/36 focused tests on Python 3.11.15, with unchanged 254-file installed inventory. Continue non-fixture run/controller admission, prospective complete scope, resumable accounting, source-owned sufficiency, provider deployment and original scientific gaps. MIT is selected and LICENSE added, without public clearance or Git mutation. PhaseA has not reached its required full stable integration boundary; do not begin FARS review yet. These component results do not authorize final freeze or public release.

The phases above describe implemented boundaries and the system fixture, not completion of every non-blocked requirement. D070 GPU-terminal integration is scoped-verified (root414 affected tests plus independent review); earlier component checkpoints record native numeric, canonical, Challenger, paper and Venue integration. Do not restart those completed slices from this older plan.

D071 source reconciliation found remaining working-path gaps. Persisted Evaluation Contract amendments, contract/design history, complete progress exclusion and paired readback are locally integrated. Operational BEST_OF_N reporting and its exposure/no-progress integration are now independently scoped-reviewed with636/636 affected tests, zero skips/failures and unchanged complete Python inventory. Preserve this new stable checkpoint privately, then continue source-owned fresh confirmatory-reserve/protocol lifecycle handling; the earlier complete backup remains historical preservation. Other material work remains: credentialed-live sensitive raw/parsed/derived/error/metadata custody; complete local scientific read isolation/resource containment/attestation; and selected literature full-text transport/application driving. General-web fallback remains decision-only; optional vendors are not all mandatory. Current limited Seatbelt launch/recovery/tamper integration passed on this host, so strict local isolation is local engineering. Preserve current ALL_SEEDS inference and all confidentiality/scientific gates. Git history must be truthful and independently public-reviewed before any push; no unfinished-vNext merge into main. Complete bounded requirement reconciliation before final source freeze. Exact evidence and remaining scope are in `.run/STATE.json` and `docs/VNEXT_REQUIREMENTS_MATRIX.md`.

The following retained final-verification obligations remain subject to the current handoff, D-079 owner triage, closed evidence and exact safety exclusions. They are not authorization for a new campaign or a stopped mechanism:

1. Maintain the current D083305-input freeze8204cca1. Retain older V9/D080 evidence at its own source identity; recheck current identity at the last mutation boundary.
2. Preserve the sealed PARTIAL Standard scan, three LOW findings, installed correction/scoped-test evidence, and exact exclusions; this is not full security PASS. Do not retry or substitute for a safety-stopped platform investigation. Source compilation is DONE for78 source/launcher files on both runtimes (159424); retain completed permissible selections at their own input identities rather than rerunning them.
3. Full captured regression remains mandatory, but do not invoke unrestricted `test-suite` while it would select exact safety-stopped probes. Use the existing whole-module captured interface for permissible modules; explicitly retain mixed stopped modules, fixture prerequisites, failures and timeouts as unresolved. Do not add a selector or suppress assertions to claim a full-suite pass.
4. DONE at structural scope: report061277c0/review64020b29 and four unchanged validators on both runtimes510c1181/093445be. Preserve the frozen report/ADR bindings; this does not turn partial behavioral criteria into PASS.
5. DONE: current guarded vNext fixture/status/verify, closureddf4f878. Preserve technical PASS and scientific MORE_EXPERIMENTS_REQUIRED, blocked paper and absent E4 separately; no unchanged rerun.
6. DONE: current legacy eight-command ladder, closurea236376b. Synthetic reproduction returns expected exit1/ARCHITECTURE_CONTROL_REPLAY_PASS; completed-run resume skips work and package readback is verified. This is not actual interrupted recovery or scientific reproduction; no unchanged rerun.
7. Exercise recovery and the exact failure-injection matrix, including malformed, substituted, raced, partial, stale, dishonest, authority-confused, transformed-credential-reflection, and deceptive-offline-transport cases; recheck zero dispatch/request-count/egress-artifact behavior, dependencies, lockfiles, network confinement, secrets, and root-path safety.
8. Finalize all narrative documentation, the architecture report, and the compact `.run` ledger using only measured pre-audit evidence. Leave the final-audit digest out of repository documents and point readers to `reports/final_audit.json`.
9. Reconcile the functional-source inventory at the final boundary and require it to match the305-input freeze. Architecture validation is already complete at the recorded scope; do not rerun unchanged inputs or delete/quarantine evidence to obtain audit acceptance.
10. Run `/opt/homebrew/bin/python3 -I -S -B scripts/scientist_one_cli.py audit-project` as the **last repository mutation**.
11. After the audit, perform read-only `-B` parsing/hashing only. Report the audit result and report digest from `reports/final_audit.json` in the external handoff; do not edit a document to backfill it.

## External blockers that must remain explicit

- Live scholarly search/full text and external novelty determination: `BLOCKED_EXTERNAL`.
- Live OpenAI provider credential/model execution: `BLOCKED_EXTERNAL`. A missing required credential blocks availability, while a present credential is deliberately barred from real-network dispatch until separately reviewed sensitive-response custody exists (`BLOCKED_LOCAL`); unverified transports receive no resolved credential.
- Real GPU cloud/CUDA execution: `BLOCKED_EXTERNAL` (the offline boundary remains `IMPLEMENTED_UNTESTED_EXTERNAL`).
- Independent review, genuine external custody, proprietary/real datasets, and E4: unavailable.
- OS-enforced sandboxing for Local Mac child execution: `BLOCKED_LOCAL` internal engineering gap.
- The run-bound gateway HMAC has no external witness and does not resist same-process/key/source compromise: `BLOCKED_LOCAL`; this does not negate its narrower caller-forgery protection.

These blockers do not prevent verification of the system fixture, but they do prevent promotion beyond `AUTONOMOUS_EXPLORATION_READY`.

## Evidence fields awaiting freeze

Current synthetic vNext/legacy identities, counts and digests are measured in the current table in `docs/VNEXT_FINAL_VERIFICATION.md`; they are no longer pending. Whole-suite acceptance remains unresolved, architecture is provisional and the last-mutation audit remains pending. Historical pre-vNext identities remain unchanged. Read the final audit identity from `reports/final_audit.json` afterward and report it externally without mutating this plan.
